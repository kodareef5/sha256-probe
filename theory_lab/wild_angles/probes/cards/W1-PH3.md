# W1-PH3 — Carry path-integral / stationary phase → derives 0.74   ·   VERDICT: INCONCLUSIVE

**Card claim:** Write the collision count as a character-sum over carry-histories; the dominant stationary-phase saddle's Gaussian fluctuation (Hessian) determinant yields the 2^{0.74N} growth exponent. Probe: enumerate exact collisions AND compute the leading-saddle character-sum estimate from the carry-coupling matrix; compare slopes.

**Probe run:** N=6,8,10 (GF(2) carry-coupling), N=4,8 (exact counts; N=8 via repo C enumerator, validated 260), N=4 (true complex character-sum exponent on the first colliding (0,b)-MSB kernel). Throttled. Mini-SHA reused from kernel; (0,9)-MSB kernel matched to the C enumerator (verified BC=BF=260 at N=8).

**Result (numbers):**
- **Empirical exponent is itself ill-pinned and noisy:** best-kernel Fig-2 least-squares slope = **0.634** (residual scatter ~1.5 bits); per-N-mod-4 classes span **0.63–1.04**; sr60-MSB slope (260→946) ≈ 0.93; documented value 0.74. So "0.74" is a coarse fit through strongly N-mod-4-oscillating data with a wide error band.
- **The cheap carry-coupling "saddle" gives c = 1.0, not 0.74.** The GF(2) finite-difference coupling J of the 3 collision conditions (de61,de62,de63) vs the 4N free-word bits has **full rank 3N** (24/24 at N=8, 30/30 at N=10) → r_eff = 3.0 → c = 4 − 3 = **1.0**, the *trivial independent-counting* exponent (= the boundary proof's "2^N expected solutions"). Hessian non-degenerate (isolated saddle exists), so the "phase too flat" kill clause is OFF.
- **No tiny-N exact c_eff obtained:** the (0,b)-MSB single-flip kernels give 0 collisions at N≤5 (the small-N collisions in Fig-2 come from a different exotic-kernel family), so the exact character-sum exponent stayed undefined at the reachable cheap N.

**Kill_criterion:** "Dead if the saddle slope deviates from 0.74 beyond fit error, or no isolated stationary points exist (phase too flat)." — **fired? cannot be evaluated** (clause 2 OFF: isolated saddle exists; clause 1 not honestly decidable by the cheap probe).

**Verdict reasoning:** INCONCLUSIVE — the cheap probe genuinely can't decide it. The carry-coupling-matrix-as-GF(2)-rank yields only c = 4 − (integer rank)/N = 1.0; it is *structurally incapable* of producing a non-integer-derived 0.74, because it linearizes away exactly the carry nonlinearity whose **complex** Gauss-sum fluctuation determinant is where a 0.74 < 1 suppression would live. (Same linearization blind spot that killed W1-PH2's RG knee.) Meanwhile the empirical target 0.74 is itself a noisy 0.63–1.04 fit, so even a correct saddle estimate near ~0.7–1.0 couldn't be cleanly confirmed/refuted against it. Neither kill clause fires (isolated saddle present; slope-deviation indeterminate), so this is not a KILL — but the prize (deriving 0.74 from a saddle) is not reached.

**Cross-check / skeptic note:** The card's own skeptic — *"carry chains are discrete; stationary phase (a continuum approx) may not dominate"* — is exactly the obstruction: a discrete GF(2) coupling has no continuum fluctuation determinant, and the honest complex character sum 2^{−3N} Σ_t Σ_W e^{2πi t·Φ(W)/2^N} with a genuine Hessian over carry histories is beyond a seconds-scale probe. What I *can* say firmly: the trivial saddle gives **1.0** (validated three ways: GF(2) rank, the boundary-proof 2^N count, and "3 independent N-bit conditions"), and the empirical exponent is **below 1** (0.63–0.93), so there IS a real sub-unity carry-suppression to explain — the card's instinct (the deficit comes from carry coupling reducing effective independence) is directionally plausible, just not captured by a linear rank. **Larger probe that would decide it:** (a) evaluate the actual complex sum and its leading-t Hessian determinant numerically at N=6–8; or (b) exact best-kernel collision counts at N=5..12 to first nail whether the empirical exponent is really 0.74 (vs 0.63 or 0.93) before attributing it to a saddle.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W1-PH3.py`

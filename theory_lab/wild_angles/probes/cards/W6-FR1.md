# W6-FR1 — Moran equation -> 0.74 from the carry-branching ratios   ·   VERDICT: KILLED

**Card claim:** Each bit-slice of the carry-difference automaton is a contraction (ratio ½, one bit); admissible-children count = local branching b_k; the similarity dimension solves the Moran equation Σ 2^{-s}·(children)=1 => s = mean log₂(branching), claimed 0.74 and = log₂(1.69) ≈ 0.757 (the "skeleton enumerator" base). *Derives* the exponent from independently-measured multiplicities.

**Probe run (adversarial — the LAST serious 0.74 candidate; DY1 sibling already KILLED):** Measured the per-bit carry-branching FOUR independent ways, none fitted to the collision count: (A) the exact modular-adder XOR-difference carry automaton (Lipmaa-Moriai), Perron eigenvalue → s; (A') the carry-only automaton with message difference fixed (the cascade "carry-realization" object); (B) the EMPIRICAL branching of the real collision-constraint solution tree (LSB→MSB w60 prefix tree) from exact full enumeration — N=4 via the repo-faithful engine, N=8 via a lab-side exact C dump (260 collisions, real carries); (C) the actual collision-growth slope re-measured from the repo's Figure-2 table; (D) the de-law carry-realization multiplicities |de58|=2^hw(db56). Throttled.

**Result (numbers):**
- (A) adder automaton: Perron λ = 4.000 → **s = 2.000** (counts both message-diff bits + carry = raw 2 DOF/slice).
- (A') carry-only automaton: λ = 8.000 → **s = 3.000** (raw carry micro-state count, 2^3).
- (B) empirical collision-tree branching: N=4 → s_pred = 0.925, geomean branch = 1.90; **N=8 → s_pred = 0.918, geomean branch = 1.89, collision-count base C^(1/N) = 2.004**; **N=10 → s_pred = 0.921, geomean branch = 1.89, count-base = 1.984** (946 collisions). Per-level b_k at N=8 = [2.00×5, 1.94, 1.77, 1.47] and at N=10 = [2.00×7, 1.95, 1.66, 1.43] — full LSB carry freedom (b=2) tapering at the MSB. Consistent ≈2^0.92 across N=4,8,10.
- (C) actual collision slope: **pooled 0.634 (base 1.552)**, intercept 5.01; per-(N mod 4)-class consecutive slopes 0.394–1.036. The target is neither 0.74 nor log₂(1.69)=0.757, and is not sharp.
- (D) de-law per-bit carry branching: 0.25 (N=4), 0.375 (N=8), 0.40 (N=10), 0.75 (N=12) = hw(db56)/N — NON-monotone, never 0.757.

**Kill_criterion:** "s_pred not within ±0.05 of 0.74 (e.g. geo-mean branching gives 2^0.5 or 2^1.0)." — **fired? YES.** The cleanest, most faithful object — the empirical carry-branching of the real collision tree at N=8 — gives geomean branch = 1.89 ≈ 2^0.92 and count-base = 2.004 = 2^1.0, i.e. exactly the **2^1.0** case the kill clause names. No construction lands in [0.69, 0.79].

**Verdict reasoning:** KILLED. The Moran/similarity dimension does NOT yield a sharp 0.757 (or 0.74) from any independently-measured branching ratio. The honest "carry-branching" of the collision constraint is ≈ 2 children per LSB slice (one free message bit), giving a growth base of ~2^1.0 and s_pred ≈ 0.92 — the explicit kill value, not 0.757. The idealized adder automata (A,A') give raw-DOF artifacts (2^2, 2^3), not 0.74. Crucially, the *target itself is soft*: the real collision-growth slope is 0.634 with per-class scatter 0.39–1.04 (prior finding #2: slope 0.673, not sharp), and the catalog's "O(1.69^N) skeleton enumerator" does not exist in the repo (the only 1.69 hits are kissat solver-log noise) — the measured base is 2.0 (cascade tail freedom) or 1.55 (pooled, the blended cross-class average), never 1.69. So 0.74-as-a-similarity-dimension is numerology: there is no sharp exponent for Moran to reproduce, and the branching it would need (2^0.757 ≈ 1.69 children/slice) is not what the carry automaton produces (it produces ≈ 2).

**Cross-check / skeptic note — the one near-0.74 coincidence, examined and dismissed:** Building the prefix-tree on the *w59* coordinate alone gives s_pred = 0.674 (N=8) and **0.743 (N=10)** — the latter lands on 0.74. This is a marginal-projection coincidence, not the Moran dimension, for two reasons: (i) it is NOT sharp — it drifts 0.674→0.743 from N=8 to N=10 (a true similarity dimension is N-stable; this is exactly the N-class scatter of prior finding #2); (ii) w59 is the *schedule-constrained* coordinate (W[61] = σ1(w59)+… , so w59's admissible values are more clustered than w57/w58/w60, which all give ≈0.91). The actual self-similar growth of the collision *set* is the count-base C^(1/N) = 2.004 (N=8) and 1.984 (N=10) → **2^1.0**, robustly, across both N — that is the dimension-analog, and it is the kill value, not 0.74. A single drifting marginal hitting 0.74 at one N is the coincidence the skeptic line warned about, not the convergence that would earn a CONFIRM. Three independent legs agree on the kill: the set growth / empirical tree (2^1.0 / 2^0.92, the kill value), the de-law multiplicities (non-monotone 0.25–0.75), and the real slope (0.634, base 1.55). This mirrors the DY1 transfer-operator kill (no normalization gives 0.74) by an orthogonal route (a sum-over-ratios Moran object, not a Perron radius), so the two failures are not the same mistake twice. The count-base = 2.004 ≈ 2 is itself meaningful: the cascade-specific collision count grows like 2^N (carry entropy = ~N bits of LSB freedom), exactly the catalog's own skeptic-line prediction ("the cascade-specific count is ~2^N"); the blended 0.74/0.673 is a cross-*kernel* average artifact, not a self-similar dimension. What would change the verdict: an independently-measured branching ratio (carry multiplicities, not a fit to collision counts) whose log₂ lands sharply in [0.70,0.78] and matches the collision slope — the data give 2^0.92 (tree) / 2^0.40 (de-law) instead.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W6-FR1.py`  (N=8 leg reads the lab-side exact dump `/tmp/coll_n8.csv` from `_w6fr_dump.c`; N=10 leg reads `/tmp/coll_n10.csv` = the repo's 946 N=10 collisions (`gap_rows.csv`) replayed through the engine to recover de58 — both EXACT)

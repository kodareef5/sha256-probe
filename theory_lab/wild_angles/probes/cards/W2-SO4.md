# W2-SO4 — Reaction-diffusion Turing instability: the 132 hard-core bits as unstable modes   ·   VERDICT: KILLED

**Card claim:** Linearize the difference map around da=0 (rotations=diffusion, carries=reaction). A Turing instability makes specific *wavelengths* grow; the 132 hard-core bits = the unstable Fourier band (self-amplifying, can't be driven to zero), forcing the HW floor. Predicts *which* positions are hard-core and ties them to the rotation constants.

**Probe run:** Two complementary linearizations of the composed 7-round tail difference map around da=0, both computing the genuine spectral object (NOT a single-bit census). (A) Real-valued empirical linear-response Jacobian J[i,k]=P(output diff-bit i flips | input diff-bit k flipped), carries INCLUDED, cascade-pinned (paths share W), averaged over conditioning states/messages; SVD gives the true growth factors σ_i, and the leading left-singular-vectors' energy on the hard-core block {a,b,e,f} tests the support claim. (B) GF(2) XOR-linearized round (`linround`, carries dropped = the strict "linearize around da=0" surrogate) composed over 7 rounds; its corank is the basis-independent linear (un)reachability dimension — the prior-#1 adjudicator. N=8 and N=10. Throttled (OMP=2, taskpolicy -b), seconds.

**Result (numbers):**
- **(B) GF(2) basis-independent linear corank = 0** at both N=8 (rank 64/64) and N=10 (rank 80/80). The linearized tail is FULL RANK — there is no frozen/uncontrollable linear subspace, and certainly no 132-dimensional "unstable band." A real linear object lands on **0**, exactly as prior #1 predicts; "132" is not here.
- **(A) The real growth spectrum is a single avalanche mode, not a 132-wavelength band.** σ_max ≈ **31.7 (N=8) / 39.6 (N=10)**, standing alone far above a continuum of σ≈1.6–3.0; #(|λ|>1) = **1** at both N. #(σ>1) = 9 (N=8) → 21 (N=10) — *drifts* with N, never a stable ~132.
- **The dominant unstable mode does NOT live on the hard core.** Leading-σ left-singular-vector energy fraction on {a,b,e,f} = **0.513 (N=8) / 0.509 (N=10)** vs chance = 0.500. Zero concentration on the 132 positions; the amplifying mode is spread uniformly across all 8 registers (generic diffusion fan-out, not a wavelength-selected band).

**Kill_criterion:** "Dead if there are no growth-factor>1 modes (da=0 fully stable → hard core is nonlinear), OR unstable-mode support doesn't align with the measured hard-core bits." — **fired? YES — clause 2 unambiguously, and clause 1 in the linear (GF(2)) reading.**

**Verdict reasoning:** KILLED. The support clause fires hard: the genuine unstable mode's energy on {a,b,e,f} is 51% = pure chance, with a *single* dominant mode (not a 132-wide band), so the "132 = unstable Fourier band, predicts which positions are hard-core" claim is false — the growing mode is wavelength-agnostic avalanche fan-out. And the linear-stability clause fires too: the basis-independent GF(2) corank is **0** (full rank → da=0 is linearly fully reachable, no self-amplifying frozen band); the σ_max≈32–40 amplifier in reading (A) is just carry-driven diffusion saturation (one bit eventually flips ~half of 8N outputs), not a Turing-selected instability. The hard core is therefore NONLINEAR (it lives in the carry/T1+T2 structure, per the repo), not a linear dispersion-relation prediction.

**Cross-check / skeptic note (weaponizing prior #1):** **SO4 re-commits the exact 132 corank category error flagged in #1 and already killed in CT1/RG2.** The probe was designed to catch it: a real linear (un)reachability count must be 0/128/256, and here it is **0** — so any route that "gets 132" is re-running the single-bit deterministic-control census, not deriving an unstable-mode count. I did NOT confirm a near-132: no stable, basis-independent corank/mode-count with {a,b,e,f}+4dc support exists (corank=0; support=chance). The card's own skeptic line is vindicated: "ARX difference behavior is dominated by nonlinear carry events; a linearization may miss the hard core entirely" — it does (the linear corank is 0, and the real unstable mode ignores the hard-core positions). Two independent readouts (GF(2) corank, real-valued SVD support) agree, so this isn't a single-method artifact.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W2-SO4.py`

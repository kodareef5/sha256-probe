# W5-CO1 — Backward bisimulation quotient: routes around the killed forward Myhill-Nerode   ·   VERDICT: KILLED

**Card claim:** Forward reachability-quotient is near-injective (255/260, killed). Run partition-refinement BACKWARD from the ds=0 sink under observational equivalence; the surviving *non-singleton block = the colliding basin*, a greatest-fixpoint object the non-invertible feed-forward makes genuinely merge. Predicted: the surviving block tracks the count (260 at N=8) and *collapses to singletons at sr=61 but stays fat at sr=60*.

**Probe run:** N=4, full modular difference-state graph of the tail, layered by round depth (exact path-2 cascade words including the round-60 offset feeding W2[62],W2[63]). Ran greatest-fixpoint backward bisimulation (Paige-Tarjan, successor-signature refinement toward the ds=0 sink) at sr=60 (truncate at round 60) and sr=63 (full tail). Also computed the true colliding basin = backward-reachable set to the sink, per depth. Throttled.

**Result (numbers):**
- Distinct diff-states per depth (after rounds 56..63): **1, 1, 2, 2, 2, 32, 3454, 58362.** The diff-state space **blows up forward** through the schedule-fixed rounds 61–63 — exactly the backward blow-up prior finding #6 describes.
- True colliding basin (diff-states that can still reach ds=0) per depth: **1, 1, 2, 2, 2, 2, 1, 1.** The basin is **never fat** — 1–2 diff-states wide at every depth (pinned near ds≈0 by the cascade, de60=0), while the *non*-basin explodes to 58362.
- Backward bisimulation of the full graph: 58362 final-layer states → **2 classes**: a giant block of **58361** (all non-collisions) plus a **singleton** {ds=0} (the collision). The "surviving non-singleton block" is therefore the **NON-colliding complement**, not the colliding basin.
- The colliding sink is a **singleton at the final layer at BOTH sr=60 and sr=63.** The count 49 (N=4) / 260 (N=8) is **not tracked by any surviving block** — the 49 colliding *inputs* all funnel onto the single ds=0 node (a fiber over inputs, not a diff-state block).

**Kill_criterion:** "collapses to singletons at *both* (no richer than the killed forward quotient)." — **fired? yes (in substance).** The colliding basin is a singleton {ds=0} at the final layer at both sr=60 and sr=61; it never forms the predicted fat block. The only fat block is the degenerate DAG dead-end (non-collisions merging because they share an empty future), which is the *opposite* of the card's object and carries no collision information.

**Verdict reasoning:** KILLED. The backward quotient does NOT route around the forward Myhill–Nerode: the diff-state space blows up forward (1→32→3454→58362) just like the killed forward direction (#6), and the colliding basin, far from being a fat greatest-fixpoint block, is pinned to 1–2 diff-states by the *already-known cascade* and is a singleton at the output. The card's prediction is inverted on every axis: (i) the surviving non-singleton block is the non-collisions, not the basin; (ii) it does not track 260/49; (iii) it does not "stay fat at 60, collapse at 61" — the basin is uniformly thin and the collision is uniformly a singleton sink. The apparent 58362→2 "collapse" is the trivial final-layer dead-end degeneracy of any layered DAG, not a real reduction.

**Cross-check / skeptic note:** Could a coarser diff-coordinate (just the de-window) make the basin look fat? An earlier de-window variant did report a fat merge (13→2), but that was an artifact of throwing away the c,g registers and the carry context — the faithful full-state graph shows the merge is the dead-end complement, and the basin is thin. The genuine "merging" object here is the input-fiber over ds=0 (49 inputs → 1 sink), which is just "collisions = preimage of 0" (true of any function; the CO4 skeptic note) — not a bisimulation reduction. Independent corroboration: the forward fan-out 1→32→3454→58362 matches the boundary-proof picture (round 61 injects the diversity; rounds 62–63 amplify it).

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W5-CO1.py`

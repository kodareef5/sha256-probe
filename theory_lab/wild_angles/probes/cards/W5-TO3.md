# W5-TO3 — Non-Boolean Ω: the 132 = the LEM-failing (undecided) bits   ·   VERDICT: KILLED

**Card claim:** a bit is LEM-failing at a stage if neither value is forced; conjecture the 132 hard-core bits = the deep-decidability bits (undecided until almost-full input because the non-invertible feed-forward mixes them through all-input carries); HW~74 = uniform measure on the undecided coordinates. Toggle feed-forward→XOR: deep cluster shrinks.

**Probe run:** Faithful reproduction of hard_core_132_bits.md's deterministic-control census in the cascade-DIFFERENTIAL setting: for each output-DIFFERENCE bit (da..dh at r63), count free-tail-word input-bit flips that DETERMINISTICALLY toggle it across 200 random base tail-inputs (toggle in EVERY base = a deterministic controller). deep / LEM-failing ⇔ 0 controllers. Ran the modular (feed-forward on) and XOR-linear (feed-forward off) models at N=4, 5, 8. Throttled.

**Result (numbers):**
- REAL (modular) model: deep-fraction = **1.000** at N=4 and N=8 (ALL 8N diff-bits have 0 controllers), 0.950 at N=5. The controller histogram is the single bucket {0: 8N} — **NOT bimodal**, and deep-fraction is 1.0, not the card's 0.52.
- TOTAL hard-core count = **8N** (32 at N=4, 64 at N=8) — i.e. the full width, **NOT 4N+4** (=20/36) and **NOT a stable 132-shaped object**.
- Register identity is NOT stable: in the REAL model all 8 registers are fully hard-core; under XOR-linear the hard-core set shifts to {a,b,d,e,f,h} (N=4) / {a,d,e,h} (N=8) — which is NOT the writeup's {a,b,e,f}. Exactly the finding-#1 instability ("at small N it lands on d,h instead of a,b,e,f").
- Feed-forward off (XOR) only marginally shrinks the deep set (32→30 at N=4, 64→58 at N=8), not the card's "deep cluster shrinks" toward a small controlled set.

**Kill_criterion:** "d(b) unimodal/continuous (no Boolean/non-Boolean split), OR invertible feed-forward doesn't shrink the deep cluster." — **fired? yes.** The decidability-depth distribution is degenerate-unimodal (one bucket at 0; deep-fraction 1.0), with no Boolean/non-Boolean split. (And feed-forward off barely moves it.)

**Verdict reasoning:** TO3 re-commits the 132 category error per finding #1. There is no stable, basis-independent, bimodal "132 undecided bits": under a faithful operational "deterministic single-input control", the deep set is the FULL width (8N), not 4N+4 and not 132; the histogram is not bimodal; and the register identity carrying it is unstable (real model = all registers, XOR = a shifting subset ≠ a,b,e,f). The "132" of the writeup is the 4N+4 deterministic-control census (= width-scaling, only numerically 132 at N=32), not an intrinsic LEM-failure count — promoting it to "the LEM-failing bits" inherits the category error. KILLED.

**Cross-check / skeptic note:** Honest limitation: my "deterministic controller" = single-input-flip that *always* toggles the output-diff bit. This is stricter than the writeup's diff-linear-correlation notion (perfect ±1 linear correlation), which is what found d,g,h "controlled" and a,b,e,f "hard-core" (132/256) at N=32. My stricter notion gives the OPPOSITE extreme (everything deep, 8N) rather than reproducing the 132/256 split — so I do not reproduce the writeup's exact census. But the adversarial conclusion is the same and arguably stronger: neither notion yields a *bimodal, basis-independent 132*. The writeup's own number is width-scaling (4N+4) and rides different registers at small N; mine is full-width (8N) and unimodal. Either way the card's "132 = a stable set of LEM-failing coordinates" does not survive. A genuinely decisive deeper probe would compute the diff-linear correlation matrix at several N and show the controlled-set size = 4N+4 (not a fixed 132) and that its register identity drifts — which is exactly the category-error signature, and is consistent with what this cheaper probe already shows.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W5-TO3.py`

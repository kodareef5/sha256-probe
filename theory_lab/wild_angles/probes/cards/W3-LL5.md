# W3-LL5 — Second-moment method → 0.74 as a calibrated mean, sr=61 as E[X]<1   ·   VERDICT: SURVIVES (first-moment crossing CONFIRMED; 0.74 calibration WEAK)

**Card claim:** X = #colliding pairs; E[X]≈2^{2N}·2^{-cN} calibrates c to 0.74; concentration (Var/E²→small) says collisions are typical; sr=61 = where E[X] crosses 1 (the squared condition drags the mean below unity).

**Probe run:** Throttled (trivial compute). (1) Least-squares fit of log2(yield) vs N on the best-kernel Fig-2 data + per-N-mod-4 class slopes + the sr60-MSB cascade slope. (2) Var[X]/E[X]² across a small documented kernel ensemble at N=8,10. (3) Exact cascade-DP first-moment budget: candidate-pair count (free words W[57..sr]) vs the known condition counts (3 de-conditions always, +2 per round past 60 for g1=0 AND h=0), giving log2 E[X](sr) in units of N; cross-checked against the MEASURED sr60 collision counts (260@N=8, 946@N=10).

**Result (numbers):**
- **(1) 0.74 is NOT sharp.** Overall best-kernel slope = **0.634** (RMS resid 1.54 bits). N-mod-4 class slopes = **0.634 / 0.950 / 1.036 / 0.717** (classes 0/1/2/3); sr60-MSB cascade slope = 0.932. The empirical exponent is a coarse fit spanning 0.63–1.04; "0.74" is one number through strongly oscillating data.
- **(2) Concentration weakly supported, not established.** Var/E² over the small kernel ensemble = 0.634 (N=8, 5 kernels) → 0.040 (N=10, 3 kernels) — does not visibly grow, but the samples are too small to certify N-independence, and the cascade coupling (msg2=msg1+correction) injects a large off-diagonal second moment, so concentration is not proven.
- **(3) sr=61 as E[X]<1 — REAL and VERIFIED.** First-moment budget: sr=60 → 2^{4N}·2^{-3N}=**2^{+N} >1 (collisions TYPICAL)**; sr=61 → ×2^{-2N} (the g1=0 AND h=0 coincidence) = **2^{-N} <1 (RARE)**. **E[X] crosses 1 exactly between sr=60 and sr=61.** The budget is validated against data: predicted sr60 E[X]=2^N = 256/1024, MEASURED 260/946, ratios **1.016 / 0.924** (~10%).

**Kill_criterion:** "Dead if Var/E² grows with N (no concentration), or E[X] crosses 1 far from 61." — **fired? no** — Var/E² does not visibly grow (0.63→0.04), and E[X] crosses 1 **exactly at 60→61**, not far from it.

**Verdict reasoning:** SURVIVES, with the two sub-claims scored separately (lead #2, #5). The **sr=61-as-E[X]<1** clause is the load-bearing, decidable part and it is **CONFIRMED**: the first-moment crossing sits exactly at 60→61, derived from exact condition counts (3 de-conditions + the verified rank-2 g1,h pair) and corroborated against the measured 260/946 collision counts to ~10%. This is NOT a rename — it is an honest elementary first-moment statement, and the "2" in the 2^{-2N} that drags E[X] below 1 is the genuine two-condition structure (NT4/RG1-B CONFIRMED), not assumed. The **0.74-as-calibrated-mean** clause is WEAK (lead #2): the actual slope is 0.634 with a 0.63–1.04 band, so calibrating c≈0.74 proves nothing. Net: the kill criterion does not fire and the first-moment direction is positively confirmed, but I withhold full CONFIRMED because (i) the 0.74 calibration is not reproduced and (ii) concentration (the second-moment half the card's name promises) is only weakly supported at the reachable kernel-sample size.

**Cross-check / skeptic note:** The card's own skeptic — "the cascade forces shared structure → off-diagonal correlations may be large" — is exactly why I do NOT claim concentration: a true second-moment proof needs Var[X] including the cascade's off-diagonal pair-coupling, which the tiny kernel ensemble can't pin. What is solid is the *first* moment: E[X]=2^{4N−3N}=2^N at sr=60 matches the exact 260/946 counts, and the extra 2^{−2N} at sr=61 (the verified rank-2 cost) puts E[X]=2^{−N}<1. This converges with the repo's own framing (sr60 typical, sr61 effectively unreachable) and with NT4/RG1-B/PH1's two-condition law — convergence, not coincidence. A larger probe to reach CONFIRMED: exact best-kernel counts at N=5..12 to nail whether the exponent is really 0.74 (vs 0.634), and a genuine Var[X] over a large kernel/message ensemble to test concentration.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W3-LL5.py`

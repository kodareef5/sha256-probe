# W2-PC4 — Definability jump → "collision exists" stops being local at 61   ·   VERDICT: KILLED

**Card claim:** Bounded-variable / FO+fixed-point definability of "collision through round k" collapses at k=61 (the search boundary). Below 60 the cascade is a shift register → COLL is a bounded-radius LOCAL check; at 61 long-range schedule feedback (t−2/t−7/t−16) blows up the deciding-neighbourhood radius → COLL_61 leaves FO^k. An EF/pebble phase transition.

**Probe run:** Two complementary measures, per round k=57..63. (proxy 2, the card's primary EF quantity) the pebble-count surrogate p*(k) = # FREE message words that determine de(k)=0, via the carry-monotone schedule support (N=4,5). (proxy 1, the card's "cheaper proxy") the Gaifman-locality radius = graph distance, in the message-schedule dependency graph [edges W[r]←{W[r-2],W[r-7],W[r-15],W[r-16]}], from round k back to the nearest free word W[57..60], plus the feedback-reach = k − (earliest free word in supp(W[k])). Throttled.

**Result (numbers):**
- **Pebble count p*(k) is FLAT = 1 for ALL rounds 57→63** (both N=4 and N=5; per-round steps all 0). Every tail round has exactly one governing free schedule word — no jump at 61.
- **Gaifman feedback-reach:** 0,0,0,0 (rounds 57-60) → **2 (round 61)** → 2 → 4 (round 63). Graph-distance-to-free: 0,0,0,0 → 1 → 1 → 2. The 60→61 step (+2 in reach, +1 in distance) is the first nonzero step.
- **But the radius stays BOUNDED and small:** max graph distance to free over rounds 61-63 = **2**; reach grows at a constant ~+2 per two rounds (the t−2 recurrence hop), not a blow-up. COLL_k remains local (radius ≤2) at every round.

**Kill_criterion:** "Dead if the locality radius / p* is already growing at k≤59 (never local even when easy), OR flat through 61 (no jump)." — **fired? YES (clause 2).** The primary pebble count p* is flat through 61 (and beyond) — no jump.

**Verdict reasoning:** KILLED, and this is the subtlest of the five. The card's *primary* definability quantity — the EF/pebble count p* — is dead flat at 1 across every round, so "COLL leaves FO^k at 61" is false: the FO-variable count never changes. The *only* measure showing any step at 61 is the secondary Gaifman feedback-reach, but it (a) stays BOUNDED and small (graph radius ≤2 — COLL_k is still Gaifman-local at every round, the opposite of a blow-up out of FO+LFP), and (b) grows SMOOTHLY afterward (61→62 step 0, 62→63 step +2) at the constant rate of the t−2 schedule recurrence — i.e. it is exactly the same word-reuse / slack=−64 free-word bookkeeping that W2-PC1 found, relabeled as a "radius." A one-time genuine definability collapse would need the radius to jump to ~the full round count and p* to spike; instead radius stays O(1)-small and p* is flat. This is lead finding #4 realized: a proxy *appears* to step at 61, but the real per-round quantity is a bounded, smoothly-growing recurrence reach, not a sharp definability phase transition.

**Cross-check / skeptic note:** A clean negative with the pro-card evidence shown honestly (the feedback-reach genuinely does first move at 61 — I did not hide it). The decisive points against: the move is bounded (radius 2, still local) and continues past 61 at constant rate, and the FO-variable pebble count — the quantity the card actually names ("leaves FO^k", "round-independent pebble count") — never budges. The card's own skeptic note also bites: FO(LFP)≈PTIME and collision-finding is plausibly not PTIME, so COLL may fail FO(LFP) *everywhere* (washing out the gap) — consistent with p* being flat rather than jumping. Corroborates PH2/CT2/PC1: the 60→61 line is not a structural discontinuity; the obstruction is the carry nonlinearity (finding #1), invisible to this dependency-graph locality measure. The larger probe that could revive it: an actual EF/pebble game on the full nonlinear product structure at N=4 measuring p*(collision vs near-miss) round-by-round — but the structural support already pins p*=1 flat, so it would not jump at 61.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W2-PC4.py`

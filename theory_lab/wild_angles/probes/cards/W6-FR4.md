# W6-FR4 — Two-phase multifractal -> the 132/124 split as two local dimensions   ·   VERDICT: KILLED

**Card claim:** The output-bit invariance measure is multifractal: 124 controlled bits = the regular phase (α≈0), 132 hard-core = the singular/dominant phase (α≈1) carrying the HW mass => HW~74 = the f(α)-weighted mean. The value is whether a genuine intermediate band of partially-controllable bits exists (a new attack surface).

**Probe run:** Built the EXACT boolean deterministic-control census at width N=8,10,12: input = the 4 free tail words W57..60 (4N bits), output = the 8N state bits after round 63, via finite-difference of each input bit propagated through the full tail (rounds 57..63, exact carries, repo-faithful `_w6oc_engine`), averaged over 16 real cascade trajectory points. An input bit is a *deterministic controller* of an output bit iff flipping it flips that output bit with probability 1 across all sampled points (a stable GF(2)-linear dependence) — this is the hard_core_132 object, distinct from avalanche frequency. Throttled. ~1 s/N.

**Result (numbers):**
- Per-register deterministic-controller counts reproduce the hard-core structure: registers **a, b, e, f → 0 controllers (hard core)** at every N; **d, h → fully controlled** (count = max); **c, g → the boundary** (some bits 0, some 1–2 controllers).
- Avalanche frequency (the WRONG object) ≈ 0.50 for every register — confirming the round is a full avalanche, so "controllability" must be the *deterministic* census, not a flip frequency.
- Hard-core bit counts: 46/64 (N=8), 55/80 (N=10), 63/96 (N=12) → **hard-core fraction 0.719, 0.688, 0.656** — monotonically drifting toward 0.5 as N grows (the 4N+4-style census fraction).
- MIDDLE band (0 < controllers < max): 15 bits (N=8), 18 (N=10), 22 (N=12) — and every middle-band bit has controller-count exactly **1** ({1: 15}, {1: 18}, {1: 22}). I.e. the "spectrum" is a small-integer mass {0, 1, 2}, not a continuum.
- Generalized-dimension proxy on the controller measure: D0 ≈ 0.70–0.77 (= log(#nonzero)/log(8N), itself just the controlled-fraction in log scale); the D2 came out negative — the box-moment formula is not meaningful on this discrete integer census, i.e. there is no stable D_q spectrum to Legendre-transform.

**Kill_criterion:** "the p_i histogram is a clean two-point mass (every bit p=1 or p=0.5, nothing between) -> 'multifractal' adds nothing over the 124/132 count." — **fired? YES (in substance).** The controller-count histogram is a near-clean small-integer mass {0, 1, 2}: a dominant 0-spike (hard core) and a max-spike (controlled), with only a thin count=1 boundary layer (the c/g register edge bits). That thin layer is a width-scaling census boundary, not an intrinsic intermediate fractal phase.

**Verdict reasoning:** KILLED as "two local *dimensions*." Per prior finding #1, "132" is the deterministic-control census = {a,b,e,f} fully + a few scattered c bits = 4N+O(1), a **width-scaling count** whose fraction → 0.5 (measured: 0.72 → 0.69 → 0.66 as N: 8 → 10 → 12), not a basis-independent fractal dimension. The orchestrator's explicit bar — "Never CONFIRM a near-132/0.516 without a stable basis-independent object" — is not met: there is no stable D_q spectrum (the dimension proxy drifts with N exactly as the census fraction does, and D2 is ill-defined on the integer mass). The middle band is non-empty but is a thin count=1 boundary layer (a handful of c/g register bits whose controller count is a small integer set by carry truncation), i.e. a census artifact, NOT a genuine partially-controllable "new attack surface" with multifractal structure. The f(α) two-phase picture is a relabeling of the existing 124/132 deterministic-control split.

**Cross-check / skeptic note:** The literal kill clause ("nothing between p=1 and p=0.5") does not fire on a hair-trigger reading, because a count=1 boundary layer exists — but that layer is exactly the dc/dg edge already documented in `writeups/hard_core_132_bits.md` (dc: 28/32 controlled, 0–20 controllers; dg partial), i.e. the small-integer carry-truncation boundary, reproduced here at small N as count∈{1}. Independent corroboration that it is a census, not a dimension: (i) the hard-core *fraction* moves with N (no fixed point), (ii) the controller values are small integers, not a continuum, (iii) the "HW~74" the card invokes is 132·0.5 at N=32 = census·½, which scales as 4N·½ = 2N (a width count), not an f(α)-weighted singular-phase mass. What would change the verdict: a genuinely N-stable f(α) curve with a populated, non-integer intermediate α-band and a basis-independent box measure — none of which appears.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W6-FR4.py`

# W8-RD3 — Information bottleneck: 132 = the minimal-sufficient-statistic dimension   ·   VERDICT: KILLED (category error)

**Card claim:** min I(W;T)−β I(T;collision): the 124 controllable bits compress into T; the 132 zero-control bits are incompressible relevant info (the minimal sufficient statistic); HW~74 = the bottleneck's residual distortion (132/2+~8).

**Probe run:** Reproduced the repo's diff-linear control census (`writeups/hard_core_132_bits.md`) at N=8,10,12 using the faithful mini-SHA(N) cascade construction (`_w5co_engine`). For each of the 4N free-word bits (w57..w60), flipped it across n_base random base points (4000/3000/2000), and per round-63 output-DIFF bit recorded the correlation magnitude |2·P(flip)−1|; an output bit is "controlled" if some input bit has correlation ≥ threshold, "hard-core" otherwise. Reported across thresholds (0.99/0.9/0.5) to test method-fragility. Throttled: yes.

**Result (numbers):**
- Hard-core census **scales as 4N**: the floor at EVERY N is exactly the {a,b,e,f} registers (8 bits each): N=8 → 32, N=10 → 40, N=12 → 48. At threshold 0.99 a few c/g bits add: hc = **34 / 42 / 48** (= 4N + {2,2,0}). General form **4N+4**; at N=32 this evaluates to 4·32+4 = **132**.
- Per-register@0.5 = {a:N, b:N, e:N, f:N} EXACTLY — the four full registers, reproducing the repo's da,db,de,df hard core.
- Hard-core fraction hard-core/8N = **0.531 / 0.525 / 0.500** (→0.5 asymptotically; card's 132/256≈0.516 is the N=32 point).
- Predicted D_min = d_hc/2 = 2N+2 = 17/21/24 at N=8/10/12; at N=32, d_hc/2 = 66, +~8 = **74** = the repo's measured plateau (consistent).
- Method-fragility: the +4 dc/g bits vanish at stricter thresholds (floor = clean 4N); a strict single-bit-flip-always criterion gives ~100% hard-core (full diffusion). The 0.5 fraction is threshold-sensitive.

**Kill_criterion:** "hard-core fraction not ~0.5 across N, OR D_min not ≈ d_hc/2 ±2" — **fired? no (literal)** — the fraction is 0.50–0.53 (inside [0.45,0.58]) and D_min = d_hc/2 by construction. **But the category-error bar (prior finding #1) fired: 132 is not a stable basis-independent object.**

**Verdict reasoning:** The card's *quantitative* sub-claims survive — hard-core fraction ≈ 0.5 and D_min ≈ d_hc/2 — so the literal kill_criterion does not fire. However, per the explicit instruction (finding #1: "Never CONFIRM a near-132 without a stable basis-independent object"), this is the textbook "132 = corank" category error (now 20×). The measured object is a per-output-bit, **single-bit-flip control CENSUS** that is (a) **width-scaling** — its value is 4N+4, dominated by the four full registers {a,b,e,f} = 4N, so "132" is merely the N=32 *evaluation* of a width-scaling quantity, not a stable invariant; (b) **basis-dependent** — it counts single-bit-flip correlations in the standard register basis; and (c) **method-fragile** — the "+4" scattered bits appear/vanish with the correlation threshold (clean floor = 4N). An honest information-bottleneck minimal-sufficient-statistic *dimension* is a basis-independent object that compresses over ARBITRARY functions and would come out 0 / 8N / width-scaling — never a stable 132. By the card's own skeptic, multi-bit interactions make 132 only an UPPER bound on the true statistic. The card relabels a width-scaling control census as a basis-independent IB dimension; that relabel is a category error, not a CONFIRMED.

**Cross-check / skeptic note:** The per-register breakdown {a,b,e,f} = 4N independently reproduces the repo's `hard_core_132_bits.md` structure (da,db,de,df fully uncontrolled) — so the *census measurement* is faithful; only the IB-dimension *interpretation* fails. A skeptic defending the card would note the fraction genuinely hits ~0.5; but ~0.5 = (4N+4)/8N → 1/2 is exactly what a "4 of 8 registers" census gives at any width, carrying no IB content. What would change this to CONFIRMED: a basis-independent compression (e.g., the rank of an information-bottleneck T learned over arbitrary functions, or an SVD/mutual-information dimension invariant under output-basis change) that lands at a STABLE 132 across representations. The width-scaling 4N here is the opposite of stable.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W8-RD3.py`

# W4-IG4 — Natural-gradient plateau: the cascade IS (Fisher)⁻¹·grad, stalling at Fisher-flat   ·   VERDICT: KILLED

**Card claim:** the cascade's "zero-da first" rule is what natural gradient prescribes (attacks highest-Fisher directions first); it must stall where the remaining 132 coordinates have zero Fisher information (inverse-Fisher blows up), fixing the residual at HW~74 — unifying the static kernel (132) and the dynamic stall (74); a lower bound on all greedy difference-zeroing attacks.

**Probe run:** N=8 real mini-SHA cascade (rotations scaled as the repo C tools), throttled (OMP=2, taskpolicy -b). (1) Reconstructed the zero-da cascade trajectory (per-round register-difference HW over 400 forward schedules); (2) measured the per-round steerable/Fisher rank of the single-flip difference responses; (3) tested whether per-round progress matches a graded (Fisher)⁻¹·grad step; (4) tested whether the stall dimension (132/HW~74) equals the Fisher-flat dimension (IG1's corank).

**Result (numbers):**
- **[1 TRAJECTORY]** da (register-a difference) HW per round = **[0.00, 0.00, 0.00, 0.00]** — forced to exactly 0 in *one step* every round by find_w2. The residual is the diagonal shift cascade (c zeros at r58, d at r59, e at r60; f,g,h ride at HW~3–5): a *copy/shift* structure, not a Fisher-eigenvalue ordering.
- **[2 SPECTRUM]** within-round steerable rank = 0,3,3,3 — but this is **bounded by free-words-injected-so-far** ((r−56)·N minus da-forcing), so it reflects limited injected freedom, *not* Fisher-flatness. The decisive full-output Fisher corank (all 4 free words) is IG1's measured **0**.
- **[3 STEP-MATCH]** the cascade da-reduction is an **algebraic hard constraint** (find_w2 sets da=0 exactly), not a graded gradient step → there is no "progress ∝ 1/σ_min²" curve to fit.
- **[4 DECOUPLING]** cascade stall residual (census hard core) = **132** bits (HW~74); honest Fisher-flat dim (IG1) = **0**. **132 ≠ 0 → decouple.**

**Kill_criterion:** "no correlation between progress and the Fisher spectrum, or the stall HW decouples from the Fisher-flat dimension." — **fired? YES, on both clauses.** There is no graded progress curve at all (da-zeroing is one-step algebraic), so no Fisher-spectrum correlation exists; and the stall (132/HW~74) decouples from the Fisher-flat dimension (0).

**Verdict reasoning:** KILLED. The card's premise is removed by IG1: there is no zero-Fisher (Fisher-flat) subspace for natural gradient to stall on (honest corank = 0, full informativeness), so "the cascade stalls because inverse-Fisher blows up on a 132-dim flat" is false. Mechanically, the cascade is not a natural-gradient *descent* at all — it imposes da=0 as an exact algebraic constraint each round (find_w2), with no graded step whose size could match (Fisher)⁻¹·grad. The genuine stall (the 132 hard-core → HW~74) is the **deterministic-control census / carry-T1+T2 nonlinearity**, an object that decouples from the Fisher spectrum (0). This realizes the wave preamble's own conditional: "if IG1's corank isn't ≈132, IG1/4/5 fall together" — IG1 fell (corank 0), and IG4 falls with it.

**Cross-check / skeptic note:** The within-round steerable rank (3) could be *mis*read as a Fisher-flat stall, so I flag it explicitly: it is the rank of *freedom injected up to round r* (1 word at r57, etc.), not a fundamental flatness — the full 4-word output corank is 0 (IG1, W2-CT1-Kalman). The honest comparison the card needs is final-residual-vs-final-Fisher, and that decouples (132 vs 0). The skeptic's own warning ("'is natural gradient' needs a quantitative step match, not 'both attack steerable directions first'") is exactly why this fails: no quantitative step exists. Independent corroboration: ties to W2-CT1 (corank 0/128, never 132), W4-IG1 (Fisher corank 0), and the repo's `hard_core_132_bits.md` (the 132/74 stall is a nonlinear carry census, not linear algebra). The HW~74 number is real and reproduces — but as the census residual, not a Fisher-flat dimension.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W4-IG4.py`

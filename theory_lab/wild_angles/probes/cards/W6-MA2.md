# W6-MA2 — Tutte growth rate → 0.74 via Greene's code-weight-enumerator identity   ·   VERDICT: KILLED

**Card claim:** Collisions = carry-surviving codewords of the linear constraint code C=ker(A); Greene's theorem: the weight enumerator = T(M;x,y) on the hyperbola (x−1)(y−1)=2; conjecture 0.74 = lim (1/N)log₂T at the SHA point — a Tutte growth rate, not an 11-point fit.

**Probe run:** N=4,8,10 (MSB-cascade-eligible widths; N=5,6 not eligible, find_M0=None), throttled (`OMP_NUM_THREADS=2 taskpolicy -b`). Two honest computations: (1) the MEASURED collision-growth exponent from the verified MSB-kernel counts {N=4:49, N=8:260, N=10:946} — N=8/N=10 reproduced exactly this session from the lab-side dumper (`/tmp/coll_n8.csv`=260, `/tmp/coll_n10.csv`=946; N=4 dumper=49; cross-checked by the Python engine N=4=49, N=5=0); (2) the GREENE/TUTTE growth of the linear collision code C=ker(A), where A is the GF(2) linearization of the "final difference = 0" constraint over the 4N free schedule bits — computed dim(C), |C|=2^dim, and the full weight enumerator.

**Result (numbers):**
- **(1) Measured exponent ≠ 0.74.** LS slope of log₂(count) vs N = **0.696**; the repo's own law (4N−log₂C)=3.33N ⇒ slope = 4−3.33 = **0.67**; canonical (finding #2) = **0.673**. Per-step incremental slopes scatter: N→8 = 0.602, N→10 = 0.932. Cumulative log₂(count)/N = 1.404 (N=4), 1.003 (N=8), 0.989 (N=10) → trends to **~1.0** (the 2^N cascade carry-freedom), not 0.74.
- **(2) The linear collision code is TRIVIAL.** dim(C) = **0** at N=4, 8, AND 10 (the linearized control→output map has full column rank 4N, so ker = {0}). |C| = 2^0 = 1, weight enumerator = `{weight 0: count 1}`, Greene/Tutte growth (1/N)log₂|C| = **0.000** at every N. No hyperbola point can give 0.74 from a 1-codeword code: the weight enumerator is the constant x^m, whose growth exponent is 0 regardless of (x,y).

**Kill_criterion:** "no hyperbola point gives log₂(T)/N within ±0.05 of the target AND c not stable" — **fired? YES.** The Greene/Tutte growth is identically 0 (trivial code), so no hyperbola point reaches 0.74±0.05; and the "carry tax" c = ρ_Tutte − 0.673 = 0 − 0.673 = −0.673 is not a stable derived constant — it's just minus the measured slope, because all the count lives in the carry (nonlinear) part, none in the linear code.

**Verdict reasoning:** Both clauses fire, and the card's premise collapses outright. The linear constraint code C=ker(A) is trivial ({0}) at every tested width — there are NO linear collision codewords, so "collisions = carry-surviving codewords of C" is empty: 100% of the collision count is carry-generated (nonlinear), exactly the card's own skeptic line ("the cascade-specific count is ~2^N, carry entropy = N bits"). Greene's identity applied honestly to the actual linear code therefore predicts growth **0**, the opposite of 0.74. Independently, the MEASURED collision slope is **0.67–0.70**, matching prior finding #2's 0.673 (and W6-OM2's 0.617/W6-FR1's 0.634 best-kernel LS), NOT a sharp 0.74, and with the canonical N-mod-4 scatter (0.60–0.93 per step). 0.74 is a soft, small-N, cross-kernel blend — not a Tutte/Greene growth rate of any honest code.

**Cross-check / skeptic note:** The card's skeptic note nailed it ("fix one family and use its measured exponent — a clean 1.0 would beat chasing the blended 0.74"): the MSB-family cumulative exponent trends to exactly **1.0** (carry freedom), and the linear-code growth is 0. The dim(C)=0 result is robust (GF(2) rank, basis-independent) and consistent across N=4,8,10. Corroborates W6-FR1 (MSB count-base = 2.004 = 2^1.0) and W6-OM2 (slope 0.617, scatter 0.99–2.00) by an orthogonal route (the literal Greene linear code, not a Moran/Perron object). To CONFIRM I would have needed a non-trivial code with a Greene hyperbola point at 0.74±0.05 and a stable carry-tax c; instead the code is trivial and the slope is 0.67.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W6-MA2.py`

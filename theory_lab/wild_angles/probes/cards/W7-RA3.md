# W7-RA3 — The 2^-2N wall as a Szemerédi-regularity density increment   ·   VERDICT: KILLED

**Card claim:** The collision-survival bipartite graph has a regularity partition; sr=60→61 splits a regular pair and drops survival-density by one regular-pair factor, so 2^-2N=(2^-N)² is *two* density factors (one per condition); the wall = where density falls below ε (irregular/search-dominated). **New prediction:** a spike in inter-bucket density *variance* (regularity breakdown) at the wall round.

**Probe run:** N=6,8,10. Two parts. (1) Exponent doubling + does-it-land-on-the-two-conditions: read from the repo's `gap_analysis.c` (recompiled lab-side per N with `-DN`, run in /tmp, never touching the repo) over the FULL triple space (16.78M triples at N=8) — the CSV alone (260/946 rows) is far too small for a 2^-N density, so the authoritative numbers come from the C's own full-space stdout. (2) The NEW prediction — inter-bucket density variance of the wall condition h=0 — measured from scratch over a LARGE sample (1.5M at N=8, 4.0M at N=10) bucketed by w57's high bits vs a binomial (regular-pair) null, via `_ra3_variance.py` (compute_h validated: P(h=0)=0.003915 vs authoritative 0.003931). Throttled.

**Result (numbers):** Part (1): the wall factors EXACTLY into the two conditions — P(h=0)=0.003931≈2^-8, P(g1=0)=0.003924≈2^-8, independence ratio = 0.923 (N=8, over 16.2M de61=0 hits) → 1.005 (N=10, ground truth). Rounds 57-60 are the free cascade (density 1.000, whole-cube tame); the 2^-2N drop is the round-61 schedule condition, genuine rank-2. Part (2): the variance spike is ABSENT — VAR/null = 1.711 (N=8), 1.345 (N=10): ~1 and DECREASING with N (a real Szemerédi irregular pair would give VAR/null ≫ 1, growing). The small residual is sampling noise (≈5832/3970 h=0 hits over 16 buckets).

**Kill_criterion:** "density drop is SMOOTH/single-factor (no exponent doubling), OR no variance spike at the boundary across N." — **fired? yes (prong B).** The exponent DOES double (prong A does not fire), but there is NO variance spike across N (VAR/null≈1, declining) — the OR's second prong fires.

**Verdict reasoning:** This is the one card that could have confirmed (finding #3: 2^-2N is genuinely rank-2). The two-condition structure is real and the wall lands exactly on (g1, h) with independence ~1 — but that IS finding #3, already established, not a new emergence. The Szemerédi-regularity framing contributes exactly one genuinely new, load-bearing object: the regularity-breakdown *variance spike* at the wall. That prediction is FALSE — the h=0 density is uniform across buckets (a *regular* pair, not an irregular one), with VAR/null trending to 1 as N grows. So the kill criterion's variance-spike prong fires, and per finding #3's bar a density-increment that merely *permits* 2^-2N (without a new prediction) is a RENAME, not a CONFIRMED. The honest call is KILLED: the novel claim is falsified, and the rest restates the known two-condition counting.

**Cross-check / skeptic note:** What would flip to CONFIRMED: VAR/null ≫ 1 and GROWING with N (a true irregular-pair breakdown) at the wall round specifically. Observed the opposite (1.71→1.34, →1). The two-conditions numbers are independently the repo's VERIFIED result (16M/1B-sample independence), reproduced here (P(h=0)=0.003931, ratio 0.923/1.005); my from-scratch compute_h matched to 0.4%. The "regular pair" finding is actually consistent with that verification — h is *uniform* (max-bin/mean=1.02), which is the antithesis of a regularity breakdown.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 -u wild_angles/probes/cards/W7-RA3.py` (two-conditions via lab-side gap_analysis.c) and `_ra3_variance.py` (large-sample variance-spike test — the load-bearing new-prediction check).

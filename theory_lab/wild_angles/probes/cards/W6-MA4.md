# W6-MA4 — Matroid connectivity drop → the wall as a 2-separation at round 61   ·   VERDICT: KILLED

**Card claim:** Tutte connectivity λ(M_r) is connected ≤60; at 61 W[61]=σ1(W[59])+… being schedule-determined induces a 2-SEPARATION (free block ⊕₂ schedule block), the 2 connector elements = g1,h. Gives block2_wang a target: restore 3-connectivity across 61.

**Probe run:** N=4,6 (kept tiny — card flagged moderate), throttled (`OMP_NUM_THREADS=2 taskpolicy -b`). Built the representable schedule matroid M_r on the schedule-word-bit ground set W[57..r] with the schedule recurrence (W61=σ1(W59)+…, W62=σ1(W60)+…, W63=σ1(W61)+…) as the dependencies. Computed Tutte connectivity λ(M_r) = 1 + min over BALANCED partitions of [r(X)+r(E∖X)−r(M)] (exact over all bipartitions for the small ground sets), for r=58..63; and the FORCED free(W57-60)-vs-schedule(W61-63) cut with its connector rank r(free)+r(sched)−r(M).

**Result (numbers):**
- **(A) No drop to 2 at 61.** λ(M_r) is **flat at 1** through r=58,59,60,61 at BOTH N (a connected matroid, no knee at 61). At N=6 it RISES afterward (λ = 1→2→6 at r=61→62→63) — the opposite of a drop, and any change is at 62/63, not 61. Clean drop to λ=2 specifically at 60→61: **False** at both N.
- **(B) Connector ≠ 2.** The forced free-vs-schedule cut connector rank = r(free)+r(sched)−r(M) = **6 at N=4, 12 at N=6** = exactly **2N**, NOT 2. (Breakdown N=4: r(free)=6, r(sched)=12, r(M)=12 ⇒ 6; N=6: 12,18,18 ⇒ 12.) The cut connectivity λ_cut = 2N+1 (7, 13). So the free/schedule interface has a **2N-wide** connector (W[61]←W[59] is N bits, W[62]←W[60] is N bits ⇒ 2N coupling), not a 2-element {g1,h} connector.

**Kill_criterion:** "λ doesn't drop at 61, the cut doesn't align free-vs-schedule, or connector rank ≠ 2" — **fired? YES** (two of three). λ does NOT drop at 61 (flat at 1, or rises at 62/63), and the free-vs-schedule connector rank is 2N (6/12), not 2.

**Verdict reasoning:** KILLED — exactly the prior finding #4 pattern (no round-60/61 knee, now ~15×). The matroid connectivity shows **no structural 2-separation at round 61**: λ(M_r) is flat-connected through 61 and only changes (upward) at 62/63 as more pinned words pile in. The free-vs-schedule cut the card names has connector rank **2N** (the genuine schedule coupling: each pinned word W[61],W[62] draws N bits from a free word via σ1), not the claimed 2. The round map itself is identical at every r (the OC engine: rank J_r=8N, rank B_r=N at all rounds 57..63), so the only thing special about 61 is that W[61] becomes schedule-pinned — `feasible_dofs` 1→0, a DOF/enforcement bookkeeping fact, not a Tutte 2-separation. The "wall at 61" is real in the project sense (enforcement runs out there; sr≥61 must pay 2^−2N), but that is the schedule-pinning **boundary**, not a connectivity drop to a 2-element {g1,h} connector — the card's specific matroid claim is false.

**Cross-check / skeptic note:** The card's skeptic line ("at small N everything looks 2-3-separable — compute over balanced partitions with the cut location forced") is exactly what I did: balanced partitions, exact minimization, AND the forced free/schedule cut. The result is robust — λ stays at 1 (connected) through 61, and the forced cut's connector is 2N (GF(2) rank, basis-independent), consistently across N=4 and N=6. The "2" the card wants is the g1,h **coincidence-condition count** from W6-MA3 (the 2^−2N two-condition decomposition), which is a per-message arithmetic fact, NOT a matroid 2-separation connector — conflating the two is the category error. (Kernel-independent: the schedule matroid is pure recurrence, identical whether or not the MSB kernel is cascade-eligible at that N.) To CONFIRM I'd have needed λ=2 specifically at 61 with a 2-element free/schedule connector; instead λ is flat-1 and the connector is 2N.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W6-MA4.py`

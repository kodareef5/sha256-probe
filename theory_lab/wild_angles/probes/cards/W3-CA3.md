# W3-CA3 — Abstract interpretation → the 132 hard-core = a Galois precision loss   ·   VERDICT: KILLED (sound AI over-marks to 192; 132 is the deterministic census, not a Galois dimension)

**Card claim:** A sound {0,1,⊤} carry abstraction (a Galois connection) propagated from the cascade pre-conditions marks exactly the uncontrolled output bits as ⊤, recovering 132 as a *certified superset* and HW~74 as a provable residual; the ⊤-set must partition as da,db,de,df→⊤ (132) with dd,dg,dh definite (124).

**Probe run (N=32, the literal-132 width, throttled):** Two side-by-side computations. (1) A genuine SOUND {def,⊤} bit-wise abstract interpreter (known-bits/Kildall domain, γ(top)={x : x&~top=0}) over the real modular tail rounds 57–63, with sound transformers for XOR (`top=∨`), ROR/SHR (permute), Ch/Maj (`top=∨` of inputs), and modular ADD (carry contaminates every bit at/above the lowest ⊤ bit — the tightest position-independent sound carry rule); seeded da57=0, de60=0 (cascade pins), free words W[57..60]=⊤, schedule words definite. Count ⊤ bits per register at round 63. (2) The repo's deterministic single-bit control census at N=32 (40 base points) as the adjudicator.

**Result (numbers):**
- **(2) Census reproduces 132 EXACTLY:** da=db=de=df=**32** (128) + dc=**4** + dd=dg=dh=**0** ⇒ **total 132/256**. Matches `hard_core_132_bits.md` bit-for-bit (including the 4 scattered dc bits).
- **(1) The sound AI BLOWS UP to 192/256**, not 132: da,db,**dc**,de,df,**dg** all → 32 ⊤-bits; only dd,dh stay definite (0). It over-marks dc (32 vs true 4) and dg (32 vs true 0). The ⊤-count is **192 ≫ 132**.

**Kill_criterion:** "marks far more than 132 (too coarse to get dd/dg/dh definite)." — **fired? YES.** The sound propagator marks 192 and fails to keep dc/dg definite (it keeps only dd, dh).

**Verdict reasoning:** KILLED, and it re-commits the #1 category error in the precise abstract-interpretation form the lead warned about. A genuine *sound* {def,⊤} forward AI over ARX cannot recover the 132/124 partition: ⊤ is **contagious through the carry** (T (+) x = T from the lowest ⊤ bit upward) and through XOR, so once the free words seed ⊤ at bit 0, every modular-add output is fully ⊤ and the ⊤-set inflates to 192 (it smears into dc and dg, which the census shows are mostly/fully *controlled*). The only object that lands on 132 is the deterministic single-bit-control census — reproduced exactly here — which is a carry/T1+T2 nonlinearity census, **not** a Galois precision-loss dimension. So "132 = a certified abstract-interpretation superset" is false as stated: the certified superset is 192, and 132 is not basis-independent precision loss. Per finding #1, I do NOT confirm the near-132: a real, sound abstract dimension overshoots, and matching 132 would require the AI to *be* the census (a rename), not to derive a new Galois corank.

**Cross-check / skeptic note:** The census matching 132 with the exact dc=4 signature is strong evidence the N=32 tail reconstruction is correct, so the 192 over-marking is a real property of the sound abstraction, not a coding artifact. Soundness check: γ(192-mask) ⊇ the true reachable diff-set ⊇ the 132 hard core — so the AI *is* a valid certified superset, it is just not TIGHT (192 ≠ 132), which is exactly the kill. The honest counter-argument: a more refined *relational* abstract domain (tracking carry correlations / a reduced product with a linear domain) might tighten 192→132 — but (a) that is not the "{0,1,⊤} carry abstraction" the card specifies, (b) the card's distinct content was the *soundness guarantee*, and a sound guarantee gives 192 here, and (c) #1 forbids confirming a near-132 as a precision dimension without a stable basis-independent corank, which a relational domain would still not provide (the 132 is a control census over 256 specific bits, not a corank). What would revive it: a sound abstraction that provably yields exactly {da,db,de,df}+4dc as ⊤ while keeping dc-mostly/dg/dh definite — no such domain is exhibited, and the carry contamination argues against its existence.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W3-CA3.py`

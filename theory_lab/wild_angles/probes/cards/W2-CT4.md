# W2-CT4 — Minimum control-energy (LQR) → 2^-2N and 0.74 as a Gramian corank   ·   VERDICT: KILLED (mechanism; category error confirmed)

**Card claim:** enforcing one more sr-round forces the trajectory into a codim-`2N` target (the two N-bit conditions `g1=0, h=0`); over GF(2) the "energy" is a corank/count so `2^-2N = 2^-codim`, and the collision exponent `0.74` = ratio of free- to constrained-direction GF(2) volume. **Both empirical exponents become coranks/eigenvalue-products of one explicit Gramian.** Probe: `extra_codim = rank([R; g1,h-rows]) − rank(R)` — is it `2N`? free-bit slope → 0.74?

**Probe run (4 parts):** (A) measured the codim of `{g1=0, h=0}` directly on the 946 real N=10 collisions (`gap_rows.csv`); (B) tested whether `g1, h` are GF(2)-affine functionals of the schedule-diff inputs `(w57..w60)`; (C) the card's literal probe — appended `g1,h` as rows to the CT1-style linearized GF(2) reachability matrix `R` and computed `extra_codim`; (D) refit the sr60 collision-growth exponent from verified counts (N=8: **260** — re-confirmed by running the repo enumerator; N=10: **946**). Throttled; N=10 (N=12 enumerator exceeded the cheap-probe budget, not run to completion).

**Result (numbers):**
- **(A) verified part:** `g2 = g1 + h (mod 2^N)` holds for **all 946** rows; `g1`∈[2,1022] (627 distinct), `h`∈[1,1020] (620 distinct) — two near-uniform **independent N-bit modular coordinates**. So `codim = 2N → 2^-2N` is real **as a count of two N-bit modular equations**.
- **(B) category-error probe:** `g1` is **non-GF(2)-affine in 9/10 output bits**; `h` is **non-affine in 10/10 bits**. They are **modular/carry objects, not linear functionals.**
- **(C) literal card probe:** linearized `rank(R) = 80/80` (R already spans the full 8N state) ⇒ appending the constraint rows gives `extra_codim = 0`, **not 2N**.
- **(D) the 0.74:** two-point slope `log2(946/260)/(10−8) = ` **0.932** bits/N — **far from 0.74** (and from the lead's 0.673). With only 2 solid anchors and the published **non-monotone** de58/collision growth (de58: N=11→32, N=12→512, N=13→32), there is **no sharp constant**.

**Kill_criterion:** "Dead if `extra_codim ≠ 2N` (e.g. `=N`, contradicting the verified `2^-2N`), or the slope isn't 0.74." — **fired? YES, on both clauses.** The literal `extra_codim = 0 ≠ 2N`; and the slope is 0.93, not 0.74.

**Verdict reasoning:** KILLED as a Gramian-corank *mechanism* — and it commits the **category error of finding #1** in a new guise. The genuine physics is right (Part A: `2^-2N` = two independent N-bit conditions, the repo's VERIFIED result), but the card's *derivation* — that this is a `2N` corank of a reachability/observability Gramian — is false twice over: (i) the conditions `g1,h` are **modular/nonlinear** (Part B), so they are not GF(2) functionals of a linear Gramian at all; (ii) the card's own literal probe gives `extra_codim = 0` because the linearized reachability matrix is already full-rank (Part C). The "`2N` codim" is just the dimension of two scalar equations in the 2N-dim (g1,h) condition space, set **by construction** — exactly the finding-#1 trap (a real count relabeled as a linear corank). And the `0.74` is not a sharp, derivable eigenvalue: the directly-measured exponent is 0.93 ± (large), consistent with finding #2 that 0.74 is only meaningful to ±0.1.

**Cross-check / skeptic note:** Part A independently re-confirms the verified `2^-2N` mechanism (the prize is that the *count* is real), so this is not a nihilistic kill — the angle dies only as "an LQR/Gramian *corank* derivation." A defender might say "use the modular energy, not GF(2)" — but then it isn't a Gramian corank (no linear operator, no eigenvalue product), which is precisely the reframe the card sells. The 0.74 mismatch is robust: even the repo's own `GROWTH_EXPONENT=0.74` is a fit to a fuller table whose refit spreads 0.72–1.04; our cleanest two verified points land at 0.93.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W2-CT4.py` (Part D N=8 re-confirm: `gcc -O3 -march=native -Xclang -fopenmp ... backward_construct_n10.c` with `#define N 8` → 260).

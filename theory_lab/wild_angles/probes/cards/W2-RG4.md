# W2-RG4 — Prestress (second-order) rigidity → the N=10 interference + the factor-of-2   ·   VERDICT: SURVIVES (codim-2 ✓; N=10 marginal-mode ✗; "2" is linear not quadratic)

**Card claim:** sr=60 collisions are 1st-order floppy but 2nd-order rigid via cascade prestress; the N=10 constructive interference = a zero eigenvalue of the stress matrix on the flex space; sr=61 fails **two** PSD conditions ⇒ 2^-2N (the factor-of-2 = two prestress obstructions).

**Probe run:** N=8,10,12, throttled (OMP=2, taskpolicy -b). Reused shabridge primitives via a validated width-parameterized mini-SHA. At a genuine repo cascade-DP sr=60 collision (de61==0), computed: (1) the **1st-order flex space** = GF(2) nullspace of the de61 constraint Jacobian, with msg-2's casoff **held fixed** (the cascade auto-re-solving casoff makes de61≡0 along the manifold → degenerate Jacobian≡0 → a meaningless "flex = everything"; freezing casoff gives the genuine nonlinear constraint); (2) the **2nd-order prestress** = how many 1st-order flexes are *broken by carry curvature* when actually stepped (the discrete analogue of a non-PSD/marginal stress direction); (3) the **# obstructions to sr=61** = codim of {g1=0, h=0} as functions of the free coords. Validated the GF(2) nullspace helper on known cases.

**Result (numbers):**
- **1st-order flex nonempty** (prerequisite ✓): rank(J_de61|casoff-fixed) = N exactly → flex_dim = 4N−N = 3N (24, 30, 36 at N=8,10,12).
- **2nd-order form is NON-definite** ✓: **16/24, 19/30, 24/36** (≈⅔) of the 1st-order flexes are carry-obstructed (broken at a finite step) — genuine prestress curvature, not a definite/trivial form. So the first kill clause does **not** fire.
- **sr=61 obstructions = exactly 2 words** ✓: codim of {g1=0, h=0} = **2.0 words** at every N (g1-part rank N, h-part rank N). The factor-of-2 is recovered. Second kill clause does **not** fire.
- **N=10 "interference" NOT special** ✗: the obstructed-flex fraction is **flat ≈ 0.67 across N** (0.667 / 0.633 / 0.667) — N=10 shows **no** marginal/zero-mode anomaly; if anything it is marginally *lower*. No "stress eigenvalue crossing at N=10."

**Kill_criterion:** "Dead if the 2nd-order form is generically definite (no marginal modes, incl. N=10), OR the # quadratic obstructions to sr=61 isn't 2." — **fired? NO.** The 2nd-order form is non-definite (⅔ of flexes obstructed at all N), and sr=61 is codim-2.

**Verdict reasoning:** The literal kill criterion does not fire, so the angle **SURVIVES**: there is genuine 2nd-order (carry-curvature) structure on a nonempty 1st-order flex space, and sr=61 is codim-2 — both real. **But two of the card's distinctive mechanistic claims do not hold, so this is a qualified SURVIVES, not a CONFIRMED:** (1) The N=10 "constructive interference = zero stress-eigenvalue" prediction **fails** — the obstruction fraction is flat ≈0.67 across N with no N=10 anomaly, mirroring the lead's pattern that *N/round-specific* knee claims keep failing. (2) The load-bearing "**2**" is the **linear codim-2** (g1 and h are each *first-order* / rank-N in the free coords) — i.e. exactly the rank-2 / 2^-2N fact already CONFIRMED in W2-RG1 and surviving from Wave 1 — **not** a count of two *quadratic PSD* obstructions as the tensegrity framing claims. The genuine 2nd-order content is a *different, larger* number (~16–24 carry-obstructed flexes, ≈⅔ of the flex space), which does not collapse to 2. So the card correctly identifies that (i) sr=60 flexes are 1st-order-floppy/2nd-order-obstructed and (ii) sr=61 is codim-2, but it mis-attributes the "2" to the prestress form when it is the linear coincidence-variety codim (g1=0 AND h=0).

**Cross-check / skeptic note:** The critical methodological pitfall — the cascade auto-maintaining de61=0 and producing a degenerate Jacobian≡0 (which an earlier version of this probe hit, spuriously reporting flex=everything and a clean "2") — was caught and corrected by freezing casoff; the rank-0 vs rank-N difference is the tell, and the corrected rank(J)=N is stable across N and across collision points. The "2nd-order obstructed flexes exist" result is robust (⅔ at every N), but it is sensitive to the choice of constraint (de61 with casoff fixed); a fuller test would expand the carries to an explicit quadratic form and read its inertia (signature) rather than a finite-step break-count — that is the larger probe needed to test whether the obstruction is literally quadratic vs higher-order. The codim-2 half independently corroborates W2-RG1 Clause B and the repo's VERIFIED 2^-2N, so that part is convergence; the N=10 marginal-mode half is a clean negative.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W2-RG4.py`

# W6-MA3 — Oriented-matroid cocircuit collapse → 2^-2N as a corank 1→2 jump   ·   VERDICT: CONFIRMED

**Card claim:** Promote carry signs to a chirotope; g1=0 and h=0 are two cocircuits with overlapping support but independent signs ⇒ joint vanishing = product 2^−N·2^−N = 2^−2N; cocircuit rank goes 1→2 at round 61. Predicts sr=62 → 2^−3N.

**Probe run:** N=10 (live, on the repo's verified gap data: 946 real sr=60 collisions, `headline_hunt/bets/coincidence_variety/gap_rows.csv`, cols w57..w60,g1,g2,h) + N=8 (repo-verified exhaustive `gap_analysis.c` numbers; 260-count cross-checked this session via the lab-side dump). Throttled. Tested the four sub-claims: (T1) are g1,h two distinct independent functionals or g1≡h up to sign; (T2) is the joint rate the product (independence ratio); (T3) one cocircuit at round 60 vs two at 61; (T4) sr=62→2^−3N structure.

**Result (numbers):**
- **(T1) Two distinct functionals.** On all 946 N=10 collisions, **g2 = g1 + h (mod 2^10)** exactly (the two-functional split). g1 and h are **NOT proportional** (no constant c with g1≡c·h): proportionality test = **False**. Distinct g1 = 627, distinct h = 620, distinct (g1,h) pairs = **946/946** (fully jointly spread). ⇒ two genuinely independent cocircuits, not one up to sign.
- **(T2) Joint rate = product.** Independence ratio R = P(g1=0 & h=0)/[P(g1=0)P(h=0)] = **1.005** at N=10 (repo VERIFIED, 1.07B hits) and **0.923** at N=8 (16.2M hits, within the ~6% sample noise of 1.0). P(g1=0)=P(h=0)=2^−N to 3 sig figs. ⇒ joint vanishing = product = **2^−2N**.
- **(T3) Rank 1→2 at round 61.** Round 60: de60=0 is one condition but a FREE word W[60] exists ⇒ enforceable (sr=60 reachable). Round 61: NO free word (W[61] schedule-pinned) ⇒ the held equation must hold by coincidence and splits into **two** independent N-bit conditions g1=0 AND h=0 ⇒ the independent-coincidence-condition count jumps to 2 at 61.
- **(T4)** sr=62 adds a further per-message match (another g=0) on top of g1,h; structurally → 2^−3N if independent (repo flags only the first step's independence is directly verified).

**Kill_criterion:** "round 61 admits only ONE cocircuit (g1≡h up to sign), joint rate ≠ product, or carry signs violate chirotope axioms" — **fired? NO.** g1 ≢ h (proportionality False, 946/946 distinct pairs) ⇒ two cocircuits, not one; joint rate = product (R=1.005); and no chirotope-axiom violation surfaced (the two functionals are well-defined sign data with overlapping support, consistent g2=g1+h).

**Verdict reasoning:** CONFIRMED, and it lands **specifically** on the two verified conditions g1, h — meeting prior finding #3's bar ("can CONFIRM if it lands on the two-conditions g1,h; a generic corank-jump that merely permits 2^−2N is a rename"). The 1→2 cocircuit jump IS the verified sr=61 decomposition (g2=g1+h, g1⊥h, ratio 1.005 ⇒ 2^−2N), reproduced live on the 946-collision gap data: g1 and h are demonstrably two independent functionals (not proportional), and their joint zero-probability is the product. This is the genuinely-rank-2 structure (#3, now 12×), now identified as the oriented-matroid 1→2 corank jump on exactly {g1,h}.

**Cross-check / skeptic note (adversarial — why this is CONFIRMED-of-the-decomposition, not new physics):** The card's own skeptic line is the binding constraint: "two independent GF(2) conditions already give 2^−2N without signs — the orientation earns its keep only if it predicts WHICH candidates have g1,h accidentally dependent." That predictive test is NOT passed — proportionality is False on every collision (g1,h are independent everywhere observed), so there is no observed "accidentally dependent" candidate the chirotope could distinguish, and the orientation/sign layer adds **no predictive content** beyond the already-established two-condition rate. So the chirotope is a faithful **rename** of the verified g1,h split; what is CONFIRMED is the corank 1→2 = {g1,h} structure and the 2^−2N rate it produces (the arithmetic/GF(2) independence does the work), not that the signs buy anything new. The verdict is CONFIRMED because the card's stated probe targets (two cocircuits, overlapping support, product rate, 1→2 at 61) are all positively reproduced and the kill criterion does not fire — but the honest read is "confirmed decomposition, sign-layer is decorative." The 2^−3N (sr=62) leg is structurally predicted, not separately verified.

**Reproduce:** `OMP_NUM_THREADS=2 taskpolicy -b python3 wild_angles/probes/cards/W6-MA3.py`

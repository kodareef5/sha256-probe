# Changelog — the living heartbeat

Append a dated line on every register change. This file growing IS the proof the lab is alive (not a
one-shot). Newest at top.

## 2026-06-03 — `wild_angles/` opened, ran 8 waves, closed at saturation
- **Pivot:** user called the conservative 2-adic/coding/lattice spine "boring" and asked for wilder,
  distributable angles in auto mode. Opened `wild_angles/` (separate from the 25-entry register): waves of
  divergent ideation, the standard cryptanalysis toolkit banned each wave, each card = a self-contained
  workload (reframe + cheapest small-N probe + kill_criterion + why-not-a-rebrand).
- **8 waves, 185 cards** across 44 territories: (1) physics/geometry/dynamics/info · (2) control/rigidity/
  circle-method/quantum-magic/self-org/proof-complexity · (3) optimal-transport/optics/Ehrhart/LLL/IET/CRN ·
  (4) info-geometry/free-prob/lattice-gauge/sheaf/causal · (5) Krohn–Rhodes/coalgebra/resistance/containers/
  CAT(0)/topos · (6) Pontryagin/o-minimality/matroid/fractal · (7) game-theory/Ramsey/FCA/quantum-walk/
  nonstandard · (8) cluster-algebra/computability/k-core/rate-distortion.
- **The result (see SYNTHESIS.md):** unrelated formalisms triangulate the same four facts — **~13** derive
  the 132 hard-core bits as the **corank/kernel/frozen-set of one linear map**, **~20** derive `2^-2N` from
  two independent conditions, **~10** derive `0.74`, **~16** isolate de58. The convergence, not any single
  card, is the deliverable.
- **Closed at wave 8 (saturation):** late territories re-narrate the known constants in fresh vocabulary
  more than they add; the falsifiability bar killed a rising fraction (Ramsey thresholds dwarf SHA dims;
  nonstandard "mostly inert"; computability "same integer three ways"). Honest call: breadth exhausted.
- **Wrap deliverable:** `DISTRIBUTION.md` — the 185 cards batched by the shared artifact each group needs
  built once (K1 GF(2) Jacobian → ~15 corank cards; K2 collision counts → ~20 "0.74"; K3 gating-count →
  ~25 "2^-2N"; K4 de-image table → ~16 de58), with run-order and the two highest-information experiments.
- **Highest-value next move (not run; out of charter):** Batch A — test *is the corank 132?* on the repo's
  near-ready `gf2_eliminate`; and Batch B's forward test — *measure the sr=62 rate* (predicted `2^-4N`).

## 2026-06-03 — Lab created, pass 1 seeded
- Scaffolded the lab as an isolated sibling of `../sha256_review/` with its own git history
  (`CHARTER.md` = read-only toward the repo, ideation not grinding, living not one-shot).
- Digested the repo (HEAD `969ed495`, 2026-05-26): 12 established framings, claims tiers, 8 open/blocked
  bets, the graveyard + negatives, and the `april28_explore` one-shot prior art (incl. its META_LESSONS).
- Ran a live community scan of the 2026 SHA-2 frontier (31-step practical / 39-step SFS / ~37-step
  automated-trail; MILP+SAT+CAS; the new Zhou et al. quantum two-block line) and grounded the fresh
  angles in primary literature (Anashin 2-adic ergodics, Klapper–Goresky FCSR span, Jutla–Patthak
  expansion code, Wallén cLAT).
- Seeded the classified register: **25 entries** = 12 repo-established baselines + 4 repo-killed (incl.
  april28's hard-non-Hensel and MPS-hostile probed negatives) + 5 fresh angles + 4 parked.
- `probe-designed` (NOT RUN) dives written for the 3 spine angles: `2adic-carry-valuation-newton`,
  `expansion-code-min-distance`, `carry-lifted-lattice`.
- Built `infra/validate_ideas.py` (every entry has a kill_criterion) and `infra/rebuild_views.py`
  (INDEX + 4 views regenerate from `ideas.yaml`). Validation: 25/25 OK.
- Adjacent-field `CATALOG.md` + 3 on-deck stubs (Gröbner/F4, persistent homology, spectral graph).
- **Next pass (on go-ahead):** run the 3 cheap spine probes on small N reusing `../sha256_review/lib/`;
  record verdicts into the dives + this changelog; promote or archive each.

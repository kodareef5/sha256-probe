<!-- GENERATED by 30_register/infra/rebuild_views.py — do not hand-edit. -->
# Shortlist — live fresh angles (generated filter)
_Status in {captured,triaged,deep-dive,probe-designed,probed} and a fresh novelty tier. Narrative version with skeptic notes: `50_meta/shortlist.md`._

### `2adic-carry-valuation-newton`  — P3, probe:cheap, probe-designed
- **2-adic valuation flow of carry propagation (FCSR-span + Newton-polygon obstruction)**
- Put a 2-adic metric on carries; a monotone v2/Newton-slope trend would be a global no-collision obstruction.
- lens: number-theory-padic · locus: carries, message-schedule, whole-function
- **dead if:** Dead if (a) FCSR/2-adic-span synthesis on the deltaW difference-sequence of a known sr=60 pair returns ~maximal span, AND (b) v2(deltaH) shows no monotone trend vs enforced rounds, AND (c) the per-round Newton polygon over Z_2 has no monotone slope. (april28 probe_03c already killed the Hensel-LIFT reading; this survives only in the span/slope readings.)
- dive: `40_deep_dives/dive_2adic-carry-valuation-newton.md`

### `expansion-code-min-distance`  — P3, probe:cheap, probe-designed
- **Minimum-distance bound on the SHA-256 message-expansion code (Jutla-Patthak technique)**
- Linearize the schedule to a GF(2) code; a min-distance bound caps how sparse a compliant difference can be.
- lens: coding-theory · locus: message-schedule, differential-trail
- **dead if:** Dead if a low-weight-codeword search (Stern/Canteaut-Chabaud) on the linearized SHA-256 expansion matrix gives a last-K-rounds min weight that NEITHER forbids sparse late trails NOR seeds a surprise light trail — i.e. it just reproduces the known degree bound.
- dive: `40_deep_dives/dive_expansion-code-min-distance.md`

### `carry-lifted-lattice`  — P3, probe:cheap, probe-designed
- **Carry-lifted lattice (LLL/BKZ over Z with unknowns = carries)**
- Lift a+b=c mod 2^32 to integers; schedule compliance = does a lattice coset hold a 0/1-carry vector.
- lens: lattice · locus: carries, message-schedule, state-cross-section
- **dead if:** Dead if the integer-lifted carry lattice for the SMALLEST open residue (sr=60->61, one round) is already so high-dimensional (carries x words x bits -> hundreds) that BKZ enumeration is hopeless, OR BKZ finds no 0/1-carry coset point where SAT also fails.
- dive: `40_deep_dives/dive_carry-lifted-lattice.md`

### `survey-propagation-factor-graph`  — P2, probe:cheap, triaged
- **Survey propagation / spin-glass analysis of the collision factor graph**
- Is the residual hard because glassy/clustered or UNSAT-dense? SP magnetizations as a branching heuristic.
- lens: statistical-physics · locus: whole-function
- **dead if:** Dead if SP fails to converge on the (dense, structured) SHA CNF — the expected failure mode — yielding no usable magnetization signal; or if magnetizations just reproduce the known backbone the repo already mines.

### `walsh-clat-pre-sat-oracle`  — P2, probe:cheap, triaged
- **Walsh/cLAT correlation as a pre-SAT trail-pruning oracle (Wallen)**
- Score which tail masks have non-negligible correlation; prune trails before spending solver time.
- lens: boolean-function · locus: differential-trail, round-function
- **dead if:** Dead if the MSB-kernel mask's modular-addition correlation (Wallen closed form) through rounds 57-63 is either uniformly near-zero (filter uninformative) OR non-zero everywhere (no pruning) — i.e. it doesn't localize the wall.


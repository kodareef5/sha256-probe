# External Review: GPT-5.4 — 20260417_0828
# Model: openai/gpt-5.4
# Reasoning: {'effort': 'high'}

## Reasoning Trace

**Exploring decision diagrams**

I’m considering using SDD with a vtree set to round or chunk separators. Zero-suppressed decision diagrams could help with sparse point sets and potentially lead to further reductions. 

I need to think about SAT oracles from BDD marginals; they might only be heuristics rather than the core concept. I should clarify what would be publication-worthy, like discussing an exact full-width sr60 certificate. There’s definitely more to explore here!**Analyzing collision states**

I’m considering the semi-free-start collision, where the internal state may be chosen. I think about how dH equals zero at the output but the starting states can be different. It seems that a multi-block approach could leverage near-collision residuals from an alternative block one, but not from certified ones like sr60. I'm trying to piece this together to better understand the implications and mechanics at play here!**Exploring project angles**

I realize this project is still moving forward, but it’s taking a different direction. I need to mention that critical pairs depend on the kernel, so scans on the MSB kernel won’t apply universally. Also, the pair count has limitations due to timeouts. A minimal correction-set estimator is necessary! There seems to be a fundamental paradox relating to future-completion quotients. I’m considering testing Myhill-Nerode classes at N8. If we compute those and notice many collisions, it might indicate some interesting outcomes!**Analyzing future-completion quotient**

I’m considering how to measure the exact future-completion quotient at N8/N10 using the full collision set. For every prefix of interleaved variables, I'll define C(α) as the set of completions leading to collisions. By merging prefixes with the same completion sets, I can find the exact Myhill-Nerode quotient of the solution language. If this quotient size is polynomial, constructing an automaton might be feasible. Additionally, using programmatic SAT with a user propagator like CaDiCaL-SHA256 could be a practical route, even if it requires significant time.**Considering publication splits**

I’m thinking about how to split the possible publications. Paper A would focus on "Structural boundary of SHA-256 cascade collisions," detailing the exact sr60, related theorems, negative results, and the BDD paradox. Paper B could be titled "Knowledge compilation and quotient transducers for ARX collision manifolds," if chunk-mode dynamic programming works well. For CRYPTO/EUROCRYPT, Paper A currently has stronger material. I should also note that there's "0% dedup in the quotient transducer" because future-completion equivalence classes can't be defined just by boundary carries. Calculating with the suffix language at N8 might yield the exact quotient size through prefixes.**Evaluating build prioritization**

I’m considering how to prioritize tasks related to the `critical_pair_rankdefect.py`. The user has 10 cores but a short 12-hour timeframe, which feels a bit tight for major builds. It seems like the best approach would be to focus on the BDD completion quotient first, as it offers a low-effort and high-information return. After that, I’d suggest working on the critical pair rankdefect predictor and then the chunk_mode_dp_n8 prototype. 

I’ll detail the BDD completion work, starting with input collisions and recording distinct sub-BDD IDs.**Analyzing kernel optimization**

I need to discuss kernel optimization at N32 and mention that I shouldn’t assume the MSB is predictable. It’s clear that the choice of kernel strongly affects counts and partial-schedule feasibility at lower widths. I’ll consider doing a systematic N32 kernel/fill sweep during candidate generation, but it’s important not to waste too much time on a long SAT race. I might also want to challenge the assumption regarding the W59 bottleneck since its marginal cardinality could be misleading.**Considering carry elimination**

I'm thinking about balancing remarks regarding the exact negative results. The idea that "carry elimination affine rank is maximal" has one caveat: while the rank in carry plus message coordinates at N4 may not eliminate nonlinear changes, it suggests that the right low-dimensional variables might be modes instead of carries. This points out that linear algebra may be ineffective in the raw basis but could still hold value in the mode basis. It's a nuanced challenge worth addressing!**Explaining OBDD complexities**

I'm exploring the BDD paradox, noting that OBDD size can be smaller with a good order but constructing from CNF or circuits can be NP-hard and order-sensitive. Intermediate blowup often occurs, and there are cases where a function has polynomial OBDD under one ordering but exponential under another—composing small pieces can lead to blowup as well. This doesn't imply an efficient algorithm, and I need to compute the completion quotient from BDD by restricting prefixes and counting distinct residual node IDs.**Evaluating pseudocode for predictors**

I’m thinking about how to refine the pseudocode. I need to restrict variable order to achieve exact quotient widths. Then I want to build a more detailed pseudocode for the critical pair predictor.

For each sr60 collision, I envision computing the schedule residual epsilon and linearizing the local bridge. I'll also consider scoring pairs by their support and rank, simplifying as needed. It’s crucial to mention using the actual linearized bridge for effectiveness without being overly detailed.**Drafting chunk-mode DP**

I’m trying to draft the chunk-mode dynamic programming carefully. To start, I’ll use 4-bit chunks at N8 for validation, with the state consisting of {carry boundary, lin_id}. 

I need to incorporate local contexts with mutable linear constraints and recurse bits while branching on selectors and modes, then canonicalizing at the chunk boundary. It’s important to memoize the suffix DP and clarify that I shouldn't canonicalize inside the chunk; rather, I should use a rollback trail. 

I’ll also mention what results are publication-worthy, like the exact sr60 full collision and the clean structural proof for sr60/61.**Considering outline scope**

I think the current 9-section outline could potentially fit within ToSC or FSE, and maybe even CRYPTO or EUROCRYPT if I tighten the scope. I might need to consider splitting the mini-SHA results into an appendix or a separate paper. However, I’m wondering if it’s sufficient for CRYPTO or EUROCRYPT unless I include one of two strong elements: either the chunk-mode DP exact algorithm at N8/N10 with evidence toward N32 or a stronger theorem related to quotient width or knowledge compilation.

---

Here’s the blunt review.

## Executive verdict

You did **not** miss a hidden easy attack.

You **did** miss one important family of angles: **separator-based knowledge compilation / AND-OR search over mode variables**, which is different from every raw carry-DP / raw GF(2) / raw BDD construction you already tried.

My high-level assessment:

- **Most of your negative results are genuinely negative** for the exact formulations you tested.
- **The polynomial BDD is real and important**, but it is a **descriptive** result, not yet an algorithm.
- **Kernel-dependent critical pairs are a big deal**: they mean the sr=61 obstruction is **kernel-conditioned**, not just “a universal sigma1 bit-position wall.”
- The strongest next path is **not** “search faster in message space.” It is:
  1. **search over nonlinear control/mode variables**,  
  2. **solve message bits by linear algebra**,  
  3. **cache quotient states**.

If I had to compress the whole review to one sentence:

> Your solution set is compact, but your current search state is the wrong quotient.

---

## 1. Did you miss anything?

### Short answer
You did not miss a silver bullet from classical hash cryptanalysis.  
But you underexplored some adjacent frameworks.

### What’s still alive

#### A. Separator-based compilation: **YES, high priority**
This is the big missed angle.

Not:
- raw carry automaton
- raw bit-serial DP
- incremental ROBDD Apply

But:
- **factor graph**
- **linear elimination first**
- **compile the reduced nonlinear core across separators/chunks**

Think:
- AND/OR search with caching
- SDD / d-DNNF
- bucket elimination on a reduced nonlinear core
- chunk transducers with canonical residual contexts

This is the most promising constructive angle left.

#### B. Programmatic SAT / user propagators on mode variables: **YES**
You already saw:
- plain SAT can win on some instances
- raw GF(2) interpreter overhead kills FACE
- carry-aware structure matters

A good next practical angle is:
- expose **mode variables** explicitly
- use a propagator to do local GF(2) closure
- branch on modes/selectors, not message bits

This is much more plausible than hoping Kissat discovers the right semantics itself.

#### C. Projected model counting / candidate scoring: **YES**
You need a way to rank:
- candidates
- kernels
- fills
- partial schedule-enforcement patterns

without full SAT races.

Projected #SAT / model counting on reduced or chunked models is a real missing tool.

#### D. Knowledge compilation beyond ROBDD: **YES**
You’re too ROBDD-focused.

ROBDDs force a single variable order. Your problem has obvious decomposition structure:
- by rounds
- by chunks
- by carry interfaces
- by schedule bridge

That screams for:
- **SDD**
- **d-DNNF**
- **AND/OR DAGs**

Your incremental BDD failure is not a failure of all compilation.

---

### What’s probably not worth immediate time

#### E. Wang-style block-2 attack: **not dead, but not next**
I disagree with “multi-block is moot,” but I also don’t think it should be your next 12-hour build.

The correct nuanced view:
- If you deliberately search for structured block-1 residuals, then block-2 is a separate serious project.
- Your current near-collision residuals are not yet small/structured enough to make block-2 the obvious next move.

So: **not dead**, but **not tonight**.

#### F. Rebound / inbound-outbound: **conceptually relevant, practically secondary**
The problem does have a middle-interface flavor. But without the right quotient state, rebound just renames the same search barrier.

#### G. Lattice / quantum-inspired / generic ML-on-solutions: **low priority**
- Lattice: no
- Quantum-inspired: no practical lead
- ML directly on collisions: not the right abstraction

If you use ML, use it for:
- branching heuristics
- candidate ranking
- kernel/fill prioritization
- state ordering

---

## 2. Are the negative results truly negative?

### Carry-state DP: **truly negative**
Your result that raw carry-diff state width is ~89–99% of the full search space is devastating for that avenue.

That means:
- carry states are nearly injective on the search space,
- bounded width only appears **after conditioning on being a collision**.

So yes:
> raw carry-DP is dead as a search algorithm.

But add this nuance in the paper:

> This kills DP on **raw carry states**, not on **future-completion quotient states**.

That distinction matters.

---

### GF(2) linearization: **negative in the raw basis**
Also genuinely negative.

Unconditioned linearization over message bits fails because:
- modular-add carry nonlinearity is the whole game.

But your current summary is slightly too broad.

What is dead:
- raw message-space linearization
- “guess carries, then hope linear shell solves everything” in the current basis

What is still alive:
- **mode-conditioned linearization**
- **local carry-conditioned linearization**
- **linear shell after eliminating the right nonlinear controls**

So frame it as:
> Unconditioned GF(2) linearization fails; conditioned linearization remains open.

---

### Carry elimination engine: **negative for carries as coordinates**
Your affine-rank-maximal result is real, but again the nuance matters.

It rules out:
- linear algebra on **raw carry variables** as the main compression

It does **not** rule out:
- using **mode variables** instead of carries
- changing coordinates before linearization
- chunk-local linearization followed by quotienting

So this is a strong negative result on one basis, not a universal death sentence for all algebraic reduction.

---

### Incremental BDD Apply blowup: **truly negative**
This is a real theorem-shaped negative.

Small final compiled object + exponential intermediates is a classic phenomenon in symbolic verification.

This does **not** mean “no constructive path exists.”  
It means:
> the specific compositional path via ROBDD Apply is wrong.

That’s important.

---

### Backward compilation: **negative for the observable you chose**
Backward from:
- `de61/de62/de63`
- deterministic suffix diffs

was bound to be weak because you already proved the e-path is mostly deterministic.

So yes, that path is dead.

But:
- backward from the **kernel-conditioned schedule bridge**
- or from the **residual T1 condition**
is still alive.

---

### Quotient transducer 0% dedup: **negative for that quotient**
Your quotient state was too concrete.

This is one of the most important interpretation points in the whole review.

The right quotient is not:
- boundary carries + current seen-history context

It is:
- **boundary carries/modes + canonical residual future linear context**

So 0% dedup does **not** prove quotienting is useless.
It proves your quotient wasn’t aligned with future completion behavior.

---

## 3. The polynomial BDD paradox

This is one of your strongest results.

### What it means
You have shown:

- the final collision predicate admits a compact ROBDD
- but the natural circuit-construction path creates exponential intermediate objects

This is entirely plausible and well-known in symbolic computation.

### Why it is not contradictory
A small final OBDD/BDD does **not** imply:
- easy construction from CNF
- easy Apply composition
- or easy SAT search

because intermediate objects can be exponentially larger than the final object.

### Stronger interpretation
Your result says:

> the **language of collisions** is compact under a good ordering.

It does **not** say:

> the **search process** naturally discovers that compactness.

That’s the whole paradox.

---

### Important missed diagnostic: exact completion quotient from the BDD
This is a very good next build.

Since the BDD is canonical, each restricted sub-BDD node represents an exact **future-completion equivalence class**.

So from the known BDD, you can compute the exact quotient width by prefix depth.

#### Build this:
`bdd_completion_quotient.c`

#### Pseudocode
```c
Q[0] = { root };

for (depth = 0; depth < NVARS; depth++) {
    next = empty_set();

    for (node in Q[depth]) {
        if (node == FALSE || node == TRUE) {
            next.insert(node);
        } else {
            next.insert(lo(node));
            next.insert(hi(node));
        }
    }

    Q[depth+1] = unique(next);
    print(depth+1, |Q[depth+1]|);
}
```

Better version: do this grouped by bit-slice / interleaved levels.

### Why this matters
If the exact completion quotient stays small at N=8/N=10/N=12, then:
- your current quotient transducer failed because it used the wrong state,
- and a constructive automaton may exist.

If it blows up, that supports the view that the compact final BDD is not constructively accessible.

This is a high-information, low-cost build.

---

## 4. What do kernel-dependent critical pairs mean?

This is a real breakthrough.

### Main interpretation
Critical pairs are **not properties of sigma1 alone**.

They are properties of:
> the intersection between the **kernel-induced collision manifold** and the **schedule bridge**.

That’s why:
- MSB kernel gives one pair
- bit-6 kernel gives three
- other kernels differ again

### Better language
A critical pair is best understood as a:
> **minimal correction set** for the kernel-conditioned schedule bridge.

That is, after conditioning on a kernel/collision class, freeing those two coordinates is enough to restore consistency between:
- cascade-required W[60] behavior
- schedule-allowed W[60] behavior

### Practical implication
Pair count does **not** scale simply with collision count.  
It scales with the geometry of the bridge image.

That matches your data:
- some kernels with fewer collisions can have richer correction-set structure
- many timeouts mean the landscape is richer than raw SAT/UNSAT counts show

### What to build next here
Not more blind pair scans first.

Build a **critical-pair predictor**.

#### Pseudocode
```python
for kernel in kernels:
    score = defaultdict(int)

    classes = representative_sr60_classes(kernel)   # collisions or reduced carry/mode classes

    for cls in classes:
        H, r = build_linearized_schedule_bridge(cls)   # H * w60 = r over GF(2)

        for P in all_pairs(bits):
            H2, r2 = relax_coordinates(H, r, freed=P)
            if rank(H2) == rank(augmented(H2, r2)):
                score[P] += 1

    print(kernel, top_pairs(score))
```

### Immediate validation target
At N=8 this should predict:
- MSB: `(4,5)`
- bit-6: `(1,2), (1,4), (3,7)`

If it does, you have converted your newest empirical result into theory.

---

## 5. What unconventional approaches are actually worth it?

### Best unconventional but plausible direction: **compile, don’t interpret**
This is the main message.

The problem is not “search better.”  
It is “build the right finite symbolic object.”

---

### A. Mode-variable chunk compiler: **YES**
This is my top recommendation.

Search over:
- adder modes `{00,01,11}`
- `Ch` selectors
- `Maj` selectors

Reconstruct message bits by GF(2).

That’s the constructive version of your theorems.

---

### B. Programmatic SAT with mode propagator: **YES**
Use:
- explicit mode vars
- explicit carry boundary vars
- a user propagator that:
  - performs local linear closure
  - rejects inconsistent chunk states
  - caches dead suffixes

That’s much stronger than plain CNF.

---

### C. BDD/compiled object as SAT oracle: **YES**
Use N≤12 exact compiled objects to extract:
- marginals
- conditional marginals
- phase hints
- kernel/fill priors

This is not a theorem path, but it could help practically.

---

### D. Gröbner / algebraic geometry: **only after reduction**
Do **not** aim Gröbner at the raw system.

Do try it on:
- the carry/mode residual core
- after linear elimination
- on N=8/N=10 reduced models

So: medium priority, not tonight.

---

### E. Coding-theory formulation: **YES**
This is one of the best conceptual framings for sr=61:

- schedule bridge = parity-check style object
- residual mismatch = syndrome
- critical pairs = minimal correction sets

This is paper-quality interpretation.

---

### F. ML: **only as heuristic**
Use ML for:
- candidate ranking
- branching order
- state prioritization

Do not try to “learn the function.”

---

## 6. Be critical of your assumptions

Here are the main places I would push back.

### Assumption 1: polynomial BDD implies algorithmic exploitability
No. Not at all.

It implies compact **solution language**, not compact **construction path**.

---

### Assumption 2: raw carry automaton is the right object
No.

The right constructive state is:
- boundary carries/modes
- plus residual future linear context

Carries alone are the wrong quotient.

---

### Assumption 3: W[59] bottleneck marginal is algorithmic
Maybe, maybe not.

Low marginal cardinality of W1[59] is interesting, but the stronger quantity is:
\[
H(W59 \mid W57, W58)
\]
and your tree-linearity suggests this conditional entropy is the real bottleneck story.

So don’t over-interpret the marginal projection.

---

### Assumption 4: 42% invariance should be algorithmically exploitable
Not directly.

It’s a property of the **collision subset**, not the **search space**.

Your own carry-DP negative result proves that.

Publish it as solution-set structure, not as immediate algorithmic leverage.

---

### Assumption 5: sr=61 pair scans generalize from one kernel
They clearly don’t.

Your kernel-dependent critical pairs kill that assumption.

---

### Assumption 6: mini-SHA scaling tells you what N=32 will do
Only weakly.

It is useful exploratory evidence, but do not let it drive your strongest claims.

---

## 7. What did you get right? What is publication-worthy?

### Strongest publication-worthy results

#### 1. Full 32-bit sr=60 certificate
Absolutely publishable.

#### 2. Six-theorem boundary theory
This is probably your strongest “paper spine.”

The package:
- cascade diagonal
- de60 always
- three-filter equivalence
- da=de
- sr=61 cascade break
- 3x ceiling

is coherent and elegant.

#### 3. Polynomial BDD + exponential-search-space paradox
This is genuinely interesting beyond just “one more round.”

#### 4. Raw carry-DP impossibility result
Very good negative result:
- bounded width only on solutions
- near-full width on search space

That’s a real barrier theorem.

#### 5. Kernel-dependent critical pairs
This is new and sharp.

---

### Secondary but useful results
- W59 bottleneck
- cascade tree linearity
- invariant fractions
- solver landscape / 3x ceiling
- seed sensitivity lesson

These are strong supporting sections or appendices.

---

### Venue advice
#### ToSC / FSE / strong ePrint: **yes**
I think the material is there right now.

#### CRYPTO / EUROCRYPT: **maybe, if tightened**
For top-tier, I would sharpen the story to:

> exact structural boundary of the SHA-256 cascade construction

not:
- mod-4 mini-SHA ecology
- many kernel sweep anecdotes
- broad extrapolation

If you can add one constructive quotient-state algorithmic result, the case gets much stronger.

---

## 8. What should you build next? Be specific.

Here is the concrete 12-hour plan.

---

### Build 1 — highest priority
## `bdd_completion_quotient.c`
**Goal:** measure the exact future-completion quotient size from the known BDDs.

Why:
- low effort
- high information
- directly tests whether your current quotient transducer used the wrong state

#### Output
For N=8 / N=10 / N=12, by depth and by bit-slice:
- number of distinct restricted residual BDD nodes
- maybe grouped by word-slice or chunk boundary

#### Success criterion
If quotient widths stay modest, you have strong evidence the right automaton exists.

---

### Build 2
## `critical_pair_rankdefect.py`
**Goal:** explain critical pairs without blind SAT scanning.

#### Pipeline
1. take actual sr=60 collisions or representative classes
2. build linearized schedule bridge
3. test pair deletions by rank-consistency
4. predict critical pairs

#### Success criterion
Predict known N=8 MSB and bit-6 maps.

This turns your newest result into a theory result.

---

### Build 3
## `chunk_mode_dp_n8.c`
**Goal:** reconstruct the 260 N=8 MSB collisions from scratch using quotient-state DP.

### Minimum viable design
- chunk size = 4
- state = `(carry_boundary_msg1, carry_boundary_msg2, lin_id)`
- inside chunk:
  - branch on `Ch` selector
  - branch on `Maj` selector
  - branch on adder modes `{00,01,11}`
  - incremental GF(2) closure
- at chunk boundary:
  - eliminate dead vars
  - canonicalize frontier RREF
  - hash-cons to `lin_id`
- memoized suffix count

#### Success criterion
Count = 260 exactly.

If this works, it is your next real breakthrough.

---

## Pseudocode for Build 3

```python
def count_suffix(chunk_idx, state):
    key = (chunk_idx, state.carry1, state.carry2, state.lin_id)
    if key in memo:
        return memo[key]

    if chunk_idx == NUM_CHUNKS:
        memo[key] = 1 if final_accepts(state) else 0
        return memo[key]

    succ = enumerate_chunk_transitions(chunk_idx, state)
    total = 0
    for s2 in succ:
        total += count_suffix(chunk_idx + 1, s2)

    memo[key] = total
    return total


def enumerate_chunk_transitions(chunk_idx, state):
    ctx = init_local_context(state, chunk_idx)
    out = []

    def recurse_bit(bit_in_chunk):
        if bit_in_chunk == CHUNK_BITS:
            s2 = project_to_boundary_state(ctx)
            if s2 is not None:
                out.append(s2)
            return

        a,b,c,d,e,f,g,h = get_forms(ctx, bit_in_chunk)

        # Ch selector q = e
        for q in feasible_values(ctx, e):
            mark0 = ctx.mark()
            if not ctx.add_eq(e, q):
                ctx.rollback(mark0)
                continue
            ch = f if q else g

            # Maj selector p = b xor c
            for p in [0,1]:
                mark1 = ctx.mark()
                if not ctx.add_xor_eq(b, c, p):
                    ctx.rollback(mark1)
                    continue
                maj = a if p else c

                recurse_adders(bit_in_chunk, ch, maj)
                ctx.rollback(mark1)

            ctx.rollback(mark0)

    def recurse_adders(bit_in_chunk, ch, maj, add_idx=0):
        if add_idx == NUM_ACTIVE_ADDERS:
            if apply_local_equalities(ctx, bit_in_chunk, ch, maj):
                recurse_bit(bit_in_chunk + 1)
            return

        spec = build_adder_spec(ctx, bit_in_chunk, add_idx, ch, maj)
        x, y, cin1, cin2 = spec.x, spec.y, spec.cin1, spec.cin2

        for mode in ["00", "01", "11"]:
            mark = ctx.mark()
            ok = True

            if mode == "00":
                ok &= ctx.add_eq(x, 0)
                ok &= ctx.add_eq(y, 0)
                set_sum_and_carry(ctx, spec, mode, cin1, cin2)

            elif mode == "01":
                ok &= ctx.add_xor_eq(x, y, 1)
                set_sum_and_carry(ctx, spec, mode, cin1, cin2)

            else:  # "11"
                ok &= ctx.add_eq(x, 1)
                ok &= ctx.add_eq(y, 1)
                set_sum_and_carry(ctx, spec, mode, cin1, cin2)

            if ok and ctx.propagate():
                recurse_adders(bit_in_chunk, ch, maj, add_idx + 1)

            ctx.rollback(mark)

    recurse_bit(0)
    return dedup(out)
```

### Implementation advice
- **Do not** canonicalize inside the chunk
- **Do** use rollback trails, not context copies
- **Do** project away dead variables before hashing `lin_id`
- **Prototype on N=8, chunk=4**
- **Then move to N=32, chunk=8**

---

## Final bottom line

The paradox is real, but it is not mystical.

Your results strongly suggest:

- the final collision language is compact,
- raw search states are not,
- and the missing algorithmic object is a **future-completion quotient state**, not a message prefix and not a raw carry state.

So my answer to “what next?” is:

1. **measure the exact quotient from the existing BDD**
2. **predict critical pairs from rank defect**
3. **build chunked mode-DP over quotient states**

If Build 3 reproduces 260 from scratch, you’ve found the right space.

If you want, I can next write the exact `LinCtx` rollback structure and `lin_id` canonicalization scheme in C.

# External Review: GPT-5.4 — 20260417_1451
# Model: openai/gpt-5.4
# Reasoning: {'effort': 'high'}

## Reasoning Trace

**Reviewing user questions**

I'm working on a comprehensive follow-up for the user that covers eight specific questions. I need to be critical and concrete, probably organizing my thoughts into pseudocode lists A, B, C, and D. The user has submitted their implementation details, and I want to help them with actionable advice without overclaiming. 

I will address questions like whether their suggestions were implemented correctly, known techniques for dependencies, construction bottlenecks, automata theory, and concrete next steps.  It’s essential to ensure everything is synthesized clearly.**Critiquing assumptions and results**

I need to include a critique of assumptions regarding what makes research publication-worthy, focusing on the likely strongest results. For instance, the sr60 certificate, structural theorem package, and polynomial BDD with completion quotient are important, along with addressing kernel-dependent critical pairs.

I should challenge the conclusion that "rotation frontier blocks all decompositions" because it might not apply to all cases. A specific suggestion could be to consider using knowledge compilation from CNF and explore chunk-level methods. I’ll also want to state that yes, their implementation is correct while highlighting areas like BDD-derived benchmarks.**Exploring BDD distinctions**

I’m noticing an important distinction between "function having small OBDD under fixed variable ordering" and the complexity of "CNF compiled by Apply." They measured the BDD completion quotient width, which suggests a relation to the Myhill-Nerode quotient of the ordered decision process. However, I wonder if building this without exponential growth is possible—it seems hard. There are known functions with polynomial ROBDD size while still having exponential Apply intermediates in CNF compilation, which leans toward a negative conclusion.**Considering model benchmarks**

I’m thinking about using off-the-shelf D4 as a benchmark for reduced models, but a custom chunk compiler might be more effective. It seems like SDD with a vtree aligned to chunks could be the second choice. I guess knowledge compilation from existing SAT encoding would come after rewriting CNF to expose XORs and modes. So, I’d prioritize building a custom chunk-mode AND-OR/DP compiler first, then check D-DNNF/SDD sanity on reduced models, and keep BDD marginals as a low priority.**Evaluating BDD data and claims**

I need to be careful not to include unsupported claims, especially with the contradictory data I've seen around the BDD quotient and collision counts. The latest details from the user are: N8 260, N10 946, and N12 3671. 

I must clearly state the implemented suggestions: the BDD completion quotient is indeed as described, and the marginals are negative as expected. However, I’m missing the deeper BDD oracle for the exact completion quotient and state, plus the conditional marginals instead of global marginals. Unfortunately, without a compiler, I can't access N32.**Evaluating problem-solving methods**

I’m considering various methods for problem-solving. MILP might not suit exact searches but could help with trail finding. CP-SAT could work for scheduling and UNSAT cores but probably isn't better than SAT. I’ve ruled out SMT bit-vectors, and neural-guided searches seem limited to branching. Other techniques like topological data analysis and integer programming are no-goes. I’m intrigued by using PDR/IC3 for invariant synthesis, suggesting creative approaches with induction and communication complexity. However, these ideas might need more development.**Analyzing quotient transducer completion**

I need to clarify that the "quotient transducer" wasn't used correctly regarding state completion. The constructive dynamic programming I mentioned earlier can't work due to the rotation frontier issue. The crucial insight is that the quotient state must represent the future-completion residual of the compiled function, rather than relying on GF(2) forms or raw carries. There’s no efficient online method without equivalent compilation. Exploring BDD marginals with conditional marginals might help, especially if we can train from earlier quotients to guide SAT, although it's not a top priority.**Advising on BDD and transducer optimization**

I’m considering some advice regarding using two free cores. I can build `bdd_conditional_marginals.c` to calculate next-variable bias and completion counts for each quotient node at a chosen depth. This could serve as a branch heuristic in the chunk compiler or generate ML training data. However, I realize that minimizing the transducer without enumerating all states is generally not doable polynomially. Myhill-Nerode quotient computation is complex, but for fixed small-width chunks, I can use symbolic minimization and partition refinement.**Exploring crypto community developments**

I might highlight that the crypto community is likely impressed by a few key advancements: the independently verified full-width sr60 semi-free-start collision, the precise structural theory of the cascade boundary, and the polynomial BDD with an exact quotient result. There's also a negative result showing that raw carry/state-space methods fail, despite a compact solution language. This is valuable information. I might need to include a "go/stop" table to clarify things further.

---

## Executive take

You did **not** implement the suggested ideas wrong. You exposed the real gap:

- **compact solution language**
- **non-compact search process**

That gap is genuine.

My blunt view:

- For the **current single-block cascade paradigm**, you are near the natural endpoint **unless** a **quotient-state / chunk-compilation** approach works.
- For **publication**, you already have enough for a **strong paper**.
- For an actual new algorithm, the only angle I still find genuinely alive is:

> **compile the right quotient state**  
> = boundary carries/modes + canonical residual future linear context

Not raw carries, not raw message prefixes, not raw GF(2) states.

---

## 1. Did you implement prior suggestions correctly?

### Yes — especially the BDD completion quotient
The quotient result is real and important.

You measured exactly the right thing:
- distinct residual sub-BDDs after a prefix
- i.e. exact future-completion equivalence classes

That is the correct Myhill–Nerode-style quotient for the ordered collision language.

### What it means
It means an automaton of width about `#collisions` exists **for the compiled function**.

### What it does **not** mean
It does **not** imply a constructive online DP from raw arithmetic state.  
That was the missing subtlety.

So the failure of:
- GF(2)-only DP
- carry DP
- round-serial affine DP

does **not** contradict the quotient result. It proves those DPs were using the **wrong state**.

### What you missed from the BDD-oracle idea
Not global marginals. Those being flat at N≥10 is unsurprising.

The deeper oracle is:
- **conditional marginals / completion counts by quotient state**
- not unconditional per-variable bias

Global marginals are too washed out.  
Per-quotient-state next-bit biases may still be sharp.

But that’s a secondary heuristic, not the main algorithm.

---

## 2. Is the rotation frontier truly the blocker?

### Yes for forward concrete decompositions
Your proof is good **for forward-pass algorithms over concrete/locally symbolic states**.

### No for all decompositions
This is the assumption I would challenge hardest.

The relevant quantity is **not** “max rotation = 25 bits”.

It is:

> **separator width of the reduced nonlinear core**

A graph with long edges is not automatically intractable if:
- most variables can be linearly eliminated,
- and the remaining nonlinear factor graph has small separators.

So I would revise your conclusion from:

> “rotation frontier blocks all decompositions”

to:

> “rotation frontier blocks all **forward** decompositions we tested; non-forward separator-based compilation remains open.”

That’s a better and more accurate theorem.

### Conceptual answer to your automata question
The closest literature keywords are:
- finite-state transducers
- treewidth / bucket elimination
- AND-OR search with caching
- SDD / d-DNNF compilation
- cutset conditioning
- junction-tree / separator methods

“Window automata” help conceptually, but the practical object is not a raw window over bits. It is a window over:
- **nonlinear control variables**
- plus a **residual affine context**

---

## 3. Are your negative results truly negative?

### Raw carry-state DP
**Yes. Dead.**

Your width ~89–99% of `2^{4N}` means raw carries do not compress the search space.

### Raw GF(2) linearization
**Yes. Dead.**

Unconditioned linearization over message bits fails because modular addition carries are the whole problem.

### FACE / mode-branching
**Negative as implemented, not negative as a paradigm.**

Your current FACE is an **interpreter**:
- branch
- RREF
- branch
- RREF

That kills it on constants.

The still-live variant is:
- **compile chunk relations once**
- then DP over canonical chunk-state IDs

So: kill the implementation style, not the idea.

### Incremental ROBDD Apply
**Yes. Dead.**

Classic exponential intermediate blowup. Real negative.

### Backward compilation from de-values / register diffs
**Yes. Dead.**

Wrong observable. The e-path is too deterministic.

### d-DNNF / SDD / AND-OR compilation
**Not tested enough. Still alive.**

This is the one big compilation family I think you have underexplored.

---

## 4. The polynomial BDD paradox: can it lead to an algorithm?

### Yes, but probably not a polynomial-time one
This is the key distinction.

Your BDD says the collision function has compact final representation.

Your quotient data say the exact completion automaton has width about `#collisions`.

That suggests the best likely constructive complexity is something like:

\[
O(W(N)\,\mathrm{poly}(N))
\]

where \(W(N)\) is quotient width.

If \(W(N)\approx \#\text{collisions}\), and collisions grow roughly exponentially in \(N\), then:

- this is **far better** than `2^{4N}`
- but **not polynomial in N**

### Brutal truth
The polynomial BDD is likely a **descriptive** theorem, not a direct path to P.

That is still publication-grade.

### Known-theory analog
Yes, this is a known kind of phenomenon:
- small final decision diagram
- exponentially bad incremental construction path

That is standard in EDA / symbolic verification.

### What still might work
Not ROBDD Apply.  
Possibly:
- SDD with good vtree
- d-DNNF via AND-OR search
- custom chunk compiler over nonlinear modes

---

## 5. What do kernel-dependent critical pairs mean?

This is one of your strongest new results.

### Main interpretation
Critical pairs are **not properties of sigma1 alone**.

They are properties of:

> the intersection of the **kernel-induced collision manifold** with the **schedule bridge**

That is why:
- MSB kernel has `(4,5)`
- bit-6 kernel has three pairs
- other kernels differ

### Better language
A critical pair is a:

> **minimal correction set** for the kernel-conditioned schedule bridge

This is a coding-theory / linear-algebra object:
- bridge inconsistency induces a residual syndrome
- freeing two coordinates deletes enough equations to restore consistency

### Why your simple mismatch counting failed
Because support alone is not enough.  
You need **rank geometry**, not just Hamming support.

### Concrete next build
Build a **rank-defect predictor**.

#### Pseudocode
```python
for kernel in kernels:
    score = defaultdict(int)

    classes = representative_collision_or_mode_classes(kernel)

    for cls in classes:
        H, r = build_linearized_schedule_bridge(cls)  # H * w60 = r over GF(2)

        for P in all_pairs(bits):
            H2, r2 = relax_coordinates(H, r, freed=P)
            if rank(H2) == rank(augment(H2, r2)):
                score[P] += 1

    report_top_pairs(score)
```

### What it should predict
At N=8:
- MSB: `(4,5)`
- bit-6: `(1,2), (1,4), (3,7)`

If it does, you’ve converted that result from empirical to explanatory.

---

## 6. What unconventional directions are still worth trying?

### Worth trying

#### A. Separator-based compilation
Most promising remaining angle.

#### B. Programmatic SAT over mode variables
Likely best practical route if you want a solver, not just a theorem.

#### C. BDD-derived oracle features
Not raw marginals; use:
- conditional next-bit bias by quotient node
- completion counts
- branch ordering hints

#### D. Reduced residual Gröbner / PolyBoRi
Only after linear elimination and only on small residual cores.

#### E. Coding-theory framing of sr=61
Strong for paper and for critical-pair predictor.

### Not worth much right now

- raw SMT bit-vectors: you already tested enough
- MILP for exact search: no
- generic CP-SAT: unlikely better than SAT here
- topological data analysis / sheaves / category theory: no
- generic neural search over solutions: no

If you use ML, use it only for:
- candidate ranking
- branch ordering
- kernel/fill prioritization

---

## 7. What paper is this now?

### Better paper than before
Yes. The paper is now much stronger as:

> **“Compact but constructively inaccessible: the structural boundary of SHA-256 cascade collisions.”**

That is better than a mere “we got one more round” story.

### Main selling points
1. **full 32-bit sr=60 certificate**
2. **clean six-theorem structural theory of sr60/sr61**
3. **polynomial BDD / exact quotient compactness**
4. **search-space near-injectivity impossibility results**
5. **kernel-conditioned sr=61 correction-set phenomenon**

### What to tone down
- mini-SHA ecology / mod-4 narratives as main claims
- broad extrapolation to N=32 from toy widths
- anything that sounds like “we almost broke SHA-256”

### Venue realism
- **ToSC / FSE / strong ePrint:** yes now
- **CRYPTO / EUROCRYPT:** plausible if tightened, but focus the story

### My recommendation
If chunk-mode DP or separator compilation works even at N=8/10 from scratch, that becomes a second, more algorithmic paper or a major added section.

---

## 8. What should you build next?

## Highest-value path
### Build the right quotient-state compiler, not another filter

The constructive state is:

> **boundary carries/modes + canonical residual future linear context**

Not raw carries.  
Not message prefixes.  
Not register differences.

---

## Concrete builds

### Build 1 — **next 4 hours**
## `bdd_completion_quotient.c`
You already did N=8/10/12 partially. Finish and tabulate it cleanly.

**Goal**
- exact quotient width by depth
- peak width vs `#collisions`
- first exact powers-of-two region
- symmetry / bell curve

**Why**
- low effort
- high information
- paper-ready figure

**Measure**
- depth → distinct residual node count
- grouped by bit-slice if using interleaved order

**Interpretation**
This gives the exact future-completion automaton size **of the compiled function**.

---

### Build 2 — **next 4 hours**
## `critical_pair_rankdefect.py`
**Goal**
Predict critical pairs/triples from linearized bridge inconsistency, not SAT.

**Inputs**
- N=8 exact collision set (or representative classes)
- kernel choice
- schedule bridge linearization

**Output**
- rank-defect score for each pair
- top-ranked pairs
- compare to SAT map

**Success criterion**
Predict known N=8 MSB and bit-6 critical pairs.

---

### Build 3 — **next 2 days**
## `chunk_mode_dp_n8.c`
This is the big one.

**Goal**
Reconstruct the 260 N=8 MSB collisions from scratch via chunked quotient DP.

### State
```c
StateKey = {
    carry_boundary_msg1,
    carry_boundary_msg2,
    lin_id
}
```

### Local branching
- `Ch` selector: branch on `e`
- `Maj` selector: branch on `b xor c`
- binary adder mode: `{00, 01, 11}`

### Boundary action
- eliminate chunk-internal vars
- project to future frontier vars
- canonical RREF
- hash-cons to `lin_id`

### DP
Memoized suffix count or forward/backward meet.

#### Skeleton pseudocode
```python
def count_suffix(chunk_idx, state):
    key = (chunk_idx, state.carry1, state.carry2, state.lin_id)
    if key in memo:
        return memo[key]

    if chunk_idx == NUM_CHUNKS:
        memo[key] = 1 if final_accepts(state) else 0
        return memo[key]

    succ = enumerate_chunk_transitions(chunk_idx, state)
    total = 0
    for s2 in succ:
        total += count_suffix(chunk_idx + 1, s2)

    memo[key] = total
    return total
```

```python
def enumerate_chunk_transitions(chunk_idx, state):
    ctx = init_local_context(state, chunk_idx)
    out = []

    def recurse_bit(bit_in_chunk):
        if bit_in_chunk == CHUNK_BITS:
            s2 = project_to_boundary_state(ctx)
            if s2 is not None:
                out.append(s2)
            return

        a,b,c,d,e,f,g,h = get_forms(ctx, bit_in_chunk)

        for q in feasible_values(ctx, e):         # Ch selector
            m0 = ctx.mark()
            if not ctx.add_eq(e, q):
                ctx.rollback(m0); continue
            ch = f if q else g

            for p in [0,1]:                       # Maj selector
                m1 = ctx.mark()
                if not ctx.add_xor_eq(b, c, p):
                    ctx.rollback(m1); continue
                maj = a if p else c

                recurse_adders(bit_in_chunk, ch, maj)
                ctx.rollback(m1)

            ctx.rollback(m0)

    def recurse_adders(bit_in_chunk, ch, maj, add_idx=0):
        if add_idx == NUM_ACTIVE_ADDERS:
            if apply_local_equalities(ctx, bit_in_chunk, ch, maj):
                recurse_bit(bit_in_chunk + 1)
            return

        spec = build_adder_spec(ctx, bit_in_chunk, add_idx, ch, maj)
        x, y = spec.x, spec.y

        for mode in ["00", "01", "11"]:
            mark = ctx.mark()
            ok = True

            if mode == "00":
                ok &= ctx.add_eq(x, 0)
                ok &= ctx.add_eq(y, 0)
            elif mode == "01":
                ok &= ctx.add_xor_eq(x, y, 1)
            else:
                ok &= ctx.add_eq(x, 1)
                ok &= ctx.add_eq(y, 1)

            if ok:
                set_sum_and_carry(ctx, spec, mode)
                if ctx.propagate():
                    recurse_adders(bit_in_chunk, ch, maj, add_idx + 1)

            ctx.rollback(mark)

    recurse_bit(0)
    return dedup(out)
```

**Success criterion**
- exact count = 260 at N=8
- manageable number of boundary states
- no use of pre-enumerated collisions

If this works, it is your real breakthrough.

---

## 9. Lists A / B / C / D

### List A — next 4 hours
1. **Finish BDD completion quotient table** for N=8/10/12
2. **Build `critical_pair_rankdefect.py`** on N=8 MSB first
3. **Measure separator width after XOR elimination** on reduced N=8 model
4. **Stop/kill any blind SAT races with no differential information gain** on your 2 free cores

### List B — next 2 days
1. **Implement `chunk_mode_dp_n8.c`**
2. Add **rollback trail** to GF(2) context
3. Test **chunk size 4** first, then 8
4. Compare:
   - raw successor count
   - unique quotient states
   - memo hits
   - final count
5. Pilot one off-the-shelf **d-DNNF / SDD compile** on reduced N=8 if tooling available

### List C — if you had a month
1. **Custom chunk compiler / quotient transducer** for N=32
2. **Programmatic SAT with mode propagator**
3. **Reduced residual Gröbner / PolyBoRi** experiments
4. **Multi-block residual-orbit mapping** for deliberate block-1 near-collisions
5. **IC3 / interpolation-style invariant synthesis** on the bit-serial transducer (wild but plausible)

### List D — stop doing
1. Raw carry-state DP
2. Raw GF(2)-only DP
3. More blind pair/triplet SAT scans at N≥10 without predictor guidance
4. More Z3 / generic SMT benchmarking
5. More global BDD marginal extraction beyond a quick sanity check
6. Long vanilla SAT races unless a candidate is justified by structural evidence

---

## 10. Be brutally honest: are you stuck?

### On the current path: mostly yes
If by “path” you mean:
- raw carry DP
- raw GF(2) DP
- forward filtering
- register-diff suffix checks
- blind SAT racing

then yes, you are at the natural endpoint.

### But not completely
There is still one meaningful unexplored crack:

> **quotient-state compilation over mode variables and separator contexts**

If that fails to reconstruct N=8 from scratch with compact boundary state counts, then I would say the single-block cascade project is fully at its endpoint algorithmically.

### Important distinction
Even if the algorithmic project is near its endpoint, the **scientific project is not**:
- you already have a strong paper
- your negative results are valuable
- the paradox characterization is novel

So the honest answer is:

- **Are you stuck on finding a magical polynomial-time single-block sr=60 finder?**  
  Probably yes, unless chunk-mode quotient DP works.
- **Are you stuck as a research project?**  
  No. You already have publication-grade material.

---

## 11. What you got most right

The results that will impress serious cryptographers most are:

1. **The full-width sr=60 certificate**
2. **The clean sr60/sr61 structural theory**
3. **The polynomial BDD + exact completion quotient result**
4. **The negative theorem: raw carry automaton helps on solutions but not on search**
5. **Kernel-dependent critical pairs**

That combination is much stronger than “we found one hard SAT witness.”

---

If you want, I can next give you either:

1. a concrete `LinCtx` rollback / `lin_id` hashing design in C, or  
2. a stripped-down `critical_pair_rankdefect.py` implementation plan.

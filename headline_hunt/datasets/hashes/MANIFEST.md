# Manifest

SHA-256 of every file under `datasets/`. Append a row whenever a new dataset
artifact is added.

| File | SHA-256 | Added | Added by |
|---|---|---|---|
| (empty) | | | |

Maintained by `infra/manifest.py` (TODO).

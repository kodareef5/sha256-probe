# BDDs

Compressed BDD representations of collision functions (per N, per kernel).

Format: TBD per builder. Document format in the file header.

Reference: `q5_alternative_attacks/bdd_*.c` builders. The collision-list builder
is the fastest and cleanest entry point for new BDDs.

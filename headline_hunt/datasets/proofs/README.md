# Proofs

DRAT/LRAT proofs from solvers, for UNSAT certificates that go on the record.

Use `drat-trim` (in `../../../sha256_scripts/tools/drat-trim` or equivalent) to
verify before claiming any UNSAT result as VERIFIED evidence level.

Naming: `<run_id>.drat`

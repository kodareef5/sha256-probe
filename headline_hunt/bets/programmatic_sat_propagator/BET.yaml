mechanism_id: programmatic_sat_cascade_propagator
priority: 8
status: closed
owner: macbook
machines_assigned:
- macbook
compute_budget_cpu_hours: 300
current_progress: "ENGINEERING-COMPLETE (2026-04-25): Phase 2 propagator implementation\n~750 LOC C++ main + ~560 LOC unit\
  \ tests. 40+ unit tests passing,\nend-to-end runs work on real cascade-aux CNFs.\n\nPhase 2 chronology:\n  - 2A: API smoke\
  \ vs CaDiCaL 3.0.0 (commit 4b649e5)\n  - 2B: Rules 1+2 cascade-zero forcing \u2014 4x speedup on Mode A (d736ef6)\n  - 2C-Rule5:\
  \ dc_63 = dg_63 bit-equality (d3f6816)\n  - 2C-Rule4@r=61: dA[61] = dE[61] specialization (99f09ef)\n  - 2C-Rule3: dE[61..63]\
  \ = 0 three-filter (396cf5a)\n  - Varmap v2: actual register-value vars exposed (cc1dc84)\n  - Rule 4 substrate: bit-tracking\
  \ + backtrack (3424a29)\n  - Helpers + unit tests (4df69b8, 7394d98, a63fee5, 5656896)\n  - 1:many SAT-var lookup fix \u2014\
  \ partial-bit firing breakthrough (e814b3d)\n  - Modular-vs-XOR design doc (a6076d3)\n  - Encoder Option C: modular-diff\
  \ aux ripple-borrow subtractor (17c3efe)\n  - Rule 4 firing logic LIVE \u2014 157+ fires per 50k conflicts (f90ebfa)\n \
  \ - Cross-kernel firing sweep \u2014 5x variation (df398e2)\n  - 500k confirmation: front-loaded firing (4a45d20)\n\nEMPIRICAL\
  \ FINDING (kill criterion #3 fired):\n  Rule 4@r=62 fires THE SAME NUMBER of times at 50k and 500k:\n    bit-19: 209 / 209\n\
  \    bit-25: 249 / 249\n    bit-31: 201 / 201\n  All firings happen in the first ~50k conflicts; ZERO across 50k-500k.\n\
  \  Rule 4 is structurally equivalent to Mode B's preprocessing \u2014 but\n  incurs ~1.9x wall-time overhead at all measured\
  \ budgets.\n\nSTATUS = BLOCKED (was in_flight): bet stays alive ONLY if:\n  - Rule 6 at r=63 (modular Theorem 4 with TWO\
  \ varying inputs) shows\n    different firing behavior. ~400 LOC remaining.\n  - A smarter trigger fires Rule 4 incrementally\
  \ during deep search.\n  - A specific candidate identifies where vanilla cadical can't solve\n    but propagator can.\n\n\
  Recommend NOT initiating multi-hour compute experiments on Rule 4 alone.\nMode B (cascade_aux_encoding force mode) achieves\
  \ ~80% of the\npropagator's preprocessing effect with 10% of the complexity.\n\nCross-kernel diagnostic for any future re-test:\
  \ focus on bit-25 (249\nfires) and bit-19 (209 fires, 32 max bits in 1 sample) where the\npropagator's value-add is concentrated.\n"
last_heartbeat: '2026-04-28T23:15:00Z'
recent_progress_2026-04-28_PM: |
  REOPEN RATIONALE STRENGTHENED via F237 + F238 (cascade_aux_encoding
  bet's findings).

  F237 (cascade_aux_encoding bet): empirically refuted the
  "preprocessing-alone gives 200x speedup" thesis on hard sr=61
  instances. shell_eliminate_v2 reduced 28% of vars on a
  kissat-848s-timeout instance; kissat on the reduced CNF still
  timed out at 120s. Preprocessing redistributes work; doesn't
  shrink SAT-difficulty.

  This makes the IN-SEARCH PROPAGATOR direction (this bet) the
  next-best structural attack on cascade-1. Preprocessing closed;
  propagator is structurally distinct (in-search vs pre-search).

  F238: extended propagators/IPASIR_UP_API.md with three new
  implementation phases informed by F207-F217 cascade_aux structural
  analysis:
    Phase 2D - cb_decide for schedule-space bias (cheapest, 4-8 hrs)
    Phase 2E - cb_propagate for cascade-1-forced bits
    Phase 2F - cb_check_found_model rejection (most distinct)

  Concrete reopen test now defined: implement Phase 2D, run on the
  F235 hard instance (sr61_cascade_m09990bd2_f80000000_bit25, kissat
  848s timeout). If decision-priority gives ≥2x speedup on this
  hard instance, the bet's reopen is empirically justified.

  Bet remains CLOSED. Concrete reopen recipe + empirical-target
  hard instance + structurally-informed implementation phases now
  documented. Awaiting either:
  - Explicit user direction to reopen + implement
  - Yale or another machine articulating specific use case
  - Empirical demand from a different bet's pipeline

  Status as of 2026-04-28 PM:
    closed_with_reopen_recipe (was closed_no_recipe)
    structural_informedness: complete (per F207-F217)
    empirical_test_target: defined (F235 hard instance)
    implementation_estimate: 4-8 hrs minimum (Phase 2D)

  Status as of 2026-04-29 (post-F324-F326 update):
    closed_with_sharpened_reopen_recipe
    structural_informedness: complete (per F286/F311/F323-F326)
    target_bits: 132 (was 184) — F286 universal hard core
      = W*_59 (64) + W*_60 (64) + 4 anchors (W1_57[0], W2_57[0],
        W2_58[14], W2_58[26])
    empirical_test_target: F235 hard instance, ≥2x reopen criterion
    hook_priority (revised):
      - cb_decide      HIGH   (2D, 6-8 hrs)  — bias decisions to 132 bits
      - cb_add_external_clause HIGH (2D', 4-6 hrs) — inject conflict clauses
      - cb_propagate   LOW    — DOWNGRADED, F324 shows UP doesn't force
                                W*_58 bits anyway
      - cb_check_found_model MEDIUM — sanity rejection, not speedup
    impact_estimate: 2-5x on mid-difficulty TRUE sr=61, 1-2x on hard
      F235-class, <1.5x on easy sr=60 (per propagators/IPASIR_UP_API.md
      2026-04-29 update).
heartbeat_interval_days: 14
last_heartbeat: '2026-04-29T15:05:00Z'
reopen_candidate_2026_04_29_F326_sharpened: |
  F326 cross-cand validation (cascade_aux_encoding) confirms the 132-bit
  universal hard core is a CDCL-search invariant of the cascade-1
  collision problem, NOT an encoder Tseitin artifact (F324: 0/32 W2_58
  bits UP-forced; F325: 0/496 pairs UP-UNSAT; F326: pattern identical
  across 6 cands).

  This sharpens the propagator design: the structural bottleneck is
  CDCL trajectory through these 132 bits. cb_decide priority + clause
  pre-loading directly target the bottleneck. cb_propagate is no longer
  recommended (encoder doesn't pin the bits via UP, so propagator can't
  either — soundness constraint).

  Reopen now requires only: ≥1 day implementation cycle for Phase 2D+2D'.
  Empirical demand: yes — F326 motivated by 132-bit-bottleneck thesis.
  Awaiting compute decision from user (this is a multi-hour build,
  bigger than routine work).
reopen_candidate_2026_04_28: |
  Yale's F123/F125 (block2_wang) finding (2026-04-28): block-2 absorber
  candidates are STRICT radius-2 local minima even under common-mode
  flips. All bit-flip directions exhausted. Yale's stated next step:
  "structured moves that preserve late-schedule features, OR a solver
  that reasons directly over schedule words W16-W30 instead of raw
  message-bit flips."

  This is precisely what the propagator was built for. The bet's
  existing 1310 LOC + IPASIR-UP infrastructure + Rule 4-5 substrate
  could be repurposed for yale's structured-search needs.

  Concrete reopen path (if yale escalates demand):
  - Connect propagator to block2_wang absorber search loop
  - Use propagator's bit-tracking substrate to enforce schedule
    constraints during local search (escape radius-2 minima via
    propagation rather than enumeration)
  - Test on yale's score-86 fixture: does propagator-guided search
    find lower-score absorbers than pure simulated annealing?

  Also connects to principles framework's BP-Bethe synthesis (synthesis
  8 + F134-F139 calibration on cascade_aux N=32): the propagator IS a
  bespoke message-passing engine on cascade-1's Tanner graph. BP-Bethe
  is a poly-time algorithmic alternative.

  Bet stays closed until yale or another machine articulates a specific
  use case demand. F123/F125 + this entry document the reopen-candidate
  context.

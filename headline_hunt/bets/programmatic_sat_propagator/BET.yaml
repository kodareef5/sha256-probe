mechanism_id: programmatic_sat_cascade_propagator
priority: 8
status: in_flight   # Phase 2C Mode-B-parity propagator shipped; Rule 4 r=62/63 next
owner: macbook
machines_assigned: [macbook]
compute_budget_cpu_hours: 300
current_progress: |
  PHASE 2A (smoke): API verified against local CaDiCaL 3.0.0 (commit 4b649e5).
  PHASE 2B (Rules 1+2): 280 LOC C++ propagator. Mode A + propagator gives
    4x speedup vs vanilla cadical on Mode A — equals Mode B parity (commit d736ef6).
  PHASE 2C (Rules 1+2+3+5+4@r=61):
    - Rule 5  (R63.1 dc_63 = dg_63):       ships commit d3f6816
    - Rule 4 specialization (dA[61]=dE[61]): ships commit 99f09ef
    - Rule 3 (dE[61..63]=0 three-filter):    ships commit 396cf5a
    - Varmap v2 (actual register vars):      ships commit cc1dc84
    Cumulative: 400 LOC, 2.3-3.5x speedup on Mode A vs vanilla.
    Status: Mode B parity — propagator dynamically replicates Mode B's
    static unit clauses without modifying CNF.

  PHASE 2C-NEXT (Rule 4 r=62/63 — value-bearing):
    Design doc shipped: propagators/RULE4_R62_R63_DESIGN.md (commit f6a7bb7).
    Estimated ~640 LOC, 2-3 days. This is the first propagator rule that
    enforces a constraint CNF cannot express directly (modular sum with
    Sigma0/Maj on actual register values).

  Decision gate: ≥10x conflict-count reduction on sr=61 cascade-DP after
  Rule 4 r=62/63 ships, or kill the bet (per kill_criteria.md).

  Structural foundation: 105k+ records on the unified Theorem 4 (mitm_residue
  bet's commit chain, all locked).

  Build: nlohmann-json + CaDiCaL 3.0.0 (homebrew). Single-file C++.

  All 36 cross-kernel CNFs auto-emit varmap sidecars via cnfs/regenerate.sh.
  Fleet workers can pick up Rule 4 r=62/63 implementation directly from
  the design doc.

last_heartbeat: "2026-04-25T15:00:00Z"
heartbeat_interval_days: 14

---
date: 2026-04-29
bet: block2_wang × math_principles × cascade_aux_encoding cross-bet
status: SYNTHESIS — F322 basin depth-2 + yale F378-F384 W57[22:23] UNSAT core
---

# F337 + F338: F322 depth-2 enumeration + W57[22:23] core synthesis

## F337: F322 random-init basin at depth 2

Extended F336's depth-1 enumeration to depth 2: top-50 depth-1 candidates
× all 1536 depth-2 kernel-safe moves = 76,800 paths evaluated, 27.8s wall.

### Result

```
F322 base:      score=39.65, a57=5, D61=14, chart=(dh,dCh)

depth-2 BEST a57:   5  (no improvement)
depth-2 BEST D61:   5  (down from 14 — major improvement)
  path: xor_w9_b12 → xor_w11_b28
  rec:  a57=15, D61=5, chart=(dCh,dT2), score=79.10
depth-2 BEST score: 39.65 (no improvement)

Improvements over F322 base:
  a57 < 5:      0 / 76800 paths
  score < 39.65: 0 / 76800 paths
```

**F322 random-init basin's a57 floor (a57=5) is unbreakable within 2
kernel-safe moves.** The atlas score floor is also unbreakable.

D61 IS reachable down to 5 at depth 2, but at large a57 cost (a57=15)
and in a non-chamber chart. This matches yale's F374 best_d61 territory.

## Cross-machine convergence: F337 (mine) + F378 (yale)

Yale's F378 (separate path search) reached D61=4 at depth 2 from same
F322 base, in CHAMBER chart, with a57=19. My F337's selection was
optimized for atlas score; yale's beam was optimized for D61.

Combined depth-2 Pareto from F322 random-init basin:

| Source | a57 | D61 | chart | depth |
|---|---:|---:|---|---:|
| F322 base | 5 | 14 | (dh,dCh) | 0 |
| F337 best D61 | 15 | 5 | (dCh,dT2) | 2 |
| **F378 best D61** | **19** | **4** | **(dh,dCh)** | **2** |

The chamber attractor's D61=4 IS reachable within 2 kernel-safe moves of
F322's random init in chamber chart, but only at a57=19 cost. The trade-
off between a57 (guard) and D61 (defect) is precisely characterized.

## F338 W*_57[22:23] core fraction analysis

Yale's F384 minimized a 2-literal UNSAT core to:
- dW57[22] = 0
- dW57[23] = 1

Cross-checking with F286 universal-core data across 10 sr60 cands:

| Variable | Core fraction (10 cands) | Universal? |
|---|---:|:---:|
| W1_57[0]  | 1.00 | ✓ universal anchor (F286) |
| W2_57[0]  | 1.00 | ✓ universal anchor (F286) |
| **W1_57[22]** | **0.40** | NO (cand-variable) |
| **W1_57[23]** | **0.40** | NO (cand-variable) |
| **W2_57[22]** | **0.50** | NO (cand-variable) |
| **W2_57[23]** | **0.50** | NO (cand-variable) |
| W2_58[14] | 1.00 | ✓ universal anchor (F286) |
| W2_58[26] | 1.00 | ✓ universal anchor (F286) |

Yale's UNSAT core is at **cand-variable bits, NOT universal-core bits**.

## Synthesis: what this means

### Cross-bet finding

Three classes of CDCL-derived structural constraints emerge:

1. **F286 universal anchors** (10/10 cands): W1_57[0], W2_57[0],
   W2_58[14], W2_58[26]. Always pinned by CDCL across cands.

2. **F286 universal round-bits** (W*_59 + W*_60, 10/10 cands):
   the bulk 128 bits.

3. **F384 cand-variable UNSAT cores** (this finding): yale's
   2-literal UNSAT at W*_57[22:23]. NOT universal — these bits are
   cand-variable in F286's stability data. They emerge as UNSAT
   constraints for SPECIFIC cands within SPECIFIC (M1, M2) basins.

### Implication for F327 IPASIR-UP design

F327 proposed `cb_decide` priority on F286's 132-bit universal core +
`cb_add_external_clause` injection. F338 sharpens this:

- The 132-bit set is the BACKBONE — apply universally.
- The CAND-SPECIFIC UNSAT cores (like W57[22:23] = (0,1) for
  m17149975/bit31) are SECONDARY — generate them per-cand from
  conflict-guided cube search like yale's F379-F384.

The `cb_add_external_clause` hook should support BOTH: pre-load the
universal 132 bits + per-cand mined UNSAT cores.

### Implication for cross-machine flywheel

The cycle macbook F311-F322 → F324-F326 → F327 → F334 → yale F378-F384
has produced the first concrete CDCL-facing clause target (W57[22:23]
= forbidden 0,1 polarity) emerging from kernel-safe basin search.

This is the structural data the F327 propagator design needs. Ready
to feed into a Phase 2D + 2D' implementation: the propagator's
`cb_add_external_clause` hook can inject `not(dW57[22]=0 AND
dW57[23]=1)` as an early learned clause for cand m17149975/bit31.

### Implication for chamber attractor

The chamber attractor (a57=0, D61=4, chart=(dh,dCh)) is now KNOWN to
be reachable within 2 kernel-safe moves on the D61 axis (F378 D61=4)
but unreachable on the a57 axis simultaneously. The forbidden
W57[22:23]=(0,1) polarity APPEARS in F378's a57=19 D61=4 path —
suggesting that the W57[22:23] constraint is precisely the
"guard explosion" mechanism preventing simultaneous D61=4 + a57=0.

If the propagator can RULE OUT the W57[22:23]=(0,1) polarity early,
maybe the basin stops exploding the guard and a different a57 trajectory
becomes available. This is a testable hypothesis for the F327 propagator
implementation.

## What's shipped

- F337 depth-2 enumeration JSON (76,800 paths from F322 base)
- This memo combining F337 results + F338 W*_57 core fraction analysis
- 0 SAT compute beyond the depth-2 atlas evaluations

## Discipline

- ~30s wall (F337 depth-2 + F338 core analysis).
- 0 SAT compute.
- Cross-bet synthesis: tied yale's F384 conflict-guided cube finding
  to F286's universal hard core data + F311 brittleness + F327
  propagator design.
- Verified yale's F374: 303/303 PASS via F334 auditor (separate ship).

## Concrete next moves

1. **macbook**: cross-validate yale's W57[22:23]=(0,1) UNSAT against
   F324 UP test on the same CNF. If the 2-literal UP-UNSAT is reproducible,
   it's a concrete CDCL-derived clause that propagator can pre-inject.

2. **macbook or yale**: conflict-guided cube on a DIFFERENT cand
   (e.g., m3304caa0/bit10) to see if the UNSAT core is cand-specific
   or generalizes.

3. **yale**: continue the bridge cube → UNSAT core mining on F375
   (guard-repaired branch). The W57[22:23] = (1, 0) polarity is the
   "good" branch — what other constraints separate it from F378's
   guard-explosion?

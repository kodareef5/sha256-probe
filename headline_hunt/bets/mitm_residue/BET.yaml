mechanism_id: mitm_hard_residue
priority: 5
status: open
owner: macbook
machines_assigned: [macbook]
compute_budget_cpu_hours: 100   # prototype only; scaled-up post-spec
current_progress: |
  46-commit arc on 2026-04-24 (macbook). Bet's central question answered:
  hard-residue is predictable in closed-form O(1), priority MITM target
  identified, forward MITM table built as durable on-disk artifact.

  HEADLINE: closed-form predictor (h60+f60 exact, g60 lower bound via
  HW(db56_xor)) validated 35/35 as a TRUE LOWER BOUND. Mean extras 1.54,
  max 4. Predictor accurate to ±5 bits in 100% of candidates, ±3 in 89%.

  PRIORITY MITM TARGET: cand_n32_bit19_m51ca0b34_fill55555555
    - 17 empirical hard bits at round 60 (3 in f, 10 in g, 4 in h)
    - 3 bits below next-best, 12 bits below worst
    - Forward table: 2^17 = 131,072 distinct signatures, full coverage in 5M samples
    - Built and committed: datasets/bdds/priority_forward_table.bin (1.5 MB)
    - Reader: prototypes/forward_table_reader.py

  FORWARD-TABLE COLLISION SWEEP: 0 collisions in 131k witnesses, best HW=65.
  Consistent with Theorem 5 (cascade-sr=61 SAT prob 2^-32). Forward-table
  approach alone cannot find cascade-sr=61 SAT at < 2^32 samples.

  Tools shipped:
    - prototypes/forward_table_builder.py
    - prototypes/hard_residue_analyzer.py
    - prototypes/predict_hard_bits.py
    - prototypes/forward_table_reader.py

  Bet status: bet's amortization concern is FULLY characterized analytically.
  Search-space at round 60 is 2^17 for the priority candidate. But cascade-DP
  sr=61 collision discovery requires either (a) genuine MITM with backward
  analyzer reducing the 32-bit collision constraint, or (b) a fundamentally
  different mechanism (block2_wang, programmatic_sat_propagator).

  Run history: 17 kissat runs via append_run.py (all TIMEOUT, 0 audit failures).
  Empirical: SPEC's "≥10x sr=60 speedup" REFUTED at 90-min budget. SAT-speedup
  predictions retracted pending 4h+ multi-seed test (~5-8 CPU-h to settle).

  Open work that would advance the bet:
    - Backward analyzer (round-63 → round-60 collision-target derivation)
    - g60 marginal-distribution closed form (~2 days analytical)
    - 4h+ multi-seed kissat sweep on priority candidate
    - GPU N=8 prototype run (untested)

  EMPIRICAL FINDING (late 2026-04-24 session): XOR-domain dCh
  controllability (~18 bits per triple) does NOT translate to modular
  control. W1[60] sweep over 1M values for one cascade-holding triple:
  de61 distributes ~uniform-random over 2^32, 0 hits at de61=0
  (consistent with 2^-32 theoretical). Cascade-sr=61 search is
  genuinely 2^96 at the modular constraint level.

  Status review: the bet's "MITM via 17-bit hard-residue gives 2^17
  speedup" path is empirically refuted. The 17-bit signature analysis
  remains valid as a structural characterization of round-60 freedom,
  but does NOT yield a search-time speedup. Bet stays open at p5 as
  DOCUMENTATION of the cascade-DP search landscape; advancing it to
  a working attack requires either fundamentally different mechanism
  or genuine modular-difference algebra (Lipmaa-Moriai).

  Refs: results/20260424_de61_empirical_rate.md (rate measurement)
        results/20260424_de61_solver_bug.md (solver retraction)

  STRUCTURAL CLOSE-OUT (2026-04-25): The cascade-DP residual at r ∈ {61,62,63}
  is now fully characterized. Six modular constraints + two zero-diff conditions
  hold universally:
    R61.1  da_61 ≡ de_61 (mod 2^32)
    R62.1  db_62 ≡ df_62 (mod 2^32)
    R62.2  da_62 − de_62 ≡ dT2_62 (mod 2^32)   ← unified Theorem 4 r=62
    R63.1  dc_63 ≡ dg_63 (mod 2^32)
    R63.2  db_63 − df_63 ≡ dT2_62 (mod 2^32)
    R63.3  da_63 − de_63 ≡ dT2_63 (mod 2^32)   ← unified Theorem 4 r=63
    Z63d   dd_63 = 0
    Z63h   dh_63 = 0

  Validation scale (across kernel families): 60,000 fresh-sample structural
  checks + 104,800 corpus records on R63.1+R63.3 + 20,000 uniformity samples.
  Universal across 9 kernel families (bits 0,6,10,11,13,17,19,25,31).

  STRUCTURAL FLOOR DERIVATION: The 4 modular d.o.f. at r=63 (after R63.1 and
  R63.3 reduce 6 active regs to 4) implies cascade-sr=61 SAT prob = 2^(96−128)
  = 2^-32 per candidate. Matches empirical 1800 CPU-h null result and 1M-sample
  W[60] sweep finding 0 hits at de61=0. The 2^-32 hardness is structurally
  inevitable; no shortcut without a 5th independent constraint (empirically
  refuted via uniformity check) or a non-cascade-DP mechanism.

  Recommendation: PARK at p5. The bet's structural picture serves as a
  baseline for future bets: anything achieving sub-2^-32 hardness on
  cascade-DP-eligible candidates is a structural breakthrough.

  Recent results files (2026-04-25):
    results/20260425_residual_structure_complete.md (the structural picture)
    results/20260425_theorem4_unified.md (the unified Theorem 4)
    results/20260425_theorem4_unified_104k.md (104k cross-corpus)
    results/20260425_sat_prob_derivation.md (2^-32 from first principles)

  These structural results FEED forward into:
    - bets/programmatic_sat_propagator/propagators/SPEC.md (rules 4,5,6)
    - bets/block2_wang/residuals/STRUCTURAL_RERANK_NOTE.md (4-d.o.f. ranking)

  CROSS-BET UPDATE 2026-04-26 evening (from cascade_aux F12, commit b8a0a2e):
  Priority MITM target bit=19 m=0x51ca0b34 has DEFINITIVE de58 image
  min HW = 11 (full 2^32 enumeration). 0 chambers have de58=0.

  This corroborates the empirical "cascade-sr=61 = 2^96" finding here:
  the round-58 bottleneck (de58=0 needed) is unreachable for bit=19 via
  cascade-1, even with all 4.3B chambers checked. Forward-table approach
  alone cannot find cascade-sr=61 SAT — same structural fact.

  Registry's structural champion for cascade-1 reach: msb_m189b13c7
  (HW=2). NOT in this bet's priority target set, but it is the closest
  registry cand to cascade-1 collision. If a backward analyzer is built,
  msb_m189b13c7 should join bit=19 as a priority MITM target — its HW=2
  cascade-1 residual is the smallest in registry.

last_heartbeat: "2026-04-26T20:18:00Z"
heartbeat_interval_days: 14

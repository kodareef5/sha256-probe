mechanism_id: mitm_hard_residue
priority: 5
status: open
owner: macbook
machines_assigned: [macbook]
compute_budget_cpu_hours: 100   # prototype only; scaled-up post-spec
current_progress: |
  Substantive progress 2026-04-24 (macbook). All 5 next-actions from
  audit_summary either complete or actively progressing.

  ARCHITECTURE: forward MITM table keys on a 28-bit hard-residue signature
  at round 60 (NOT round 63 — that's only 64 cascade-zero bits, 192 uniform).
  Table size 2^28 entries (~5-10 GB), per-candidate. W[60] is post-hoc.

  HEADLINE FINDING (2026-04-24): ~9 of the 28 hard bits are predictable in
  CLOSED FORM from candidate state alone. h60 hard bits = bits where
  (Δe57 mod 2^i)/2^i is in [0.45, 0.55]; f60 same with Δe59=db56_mod.
  Verified 6/6 (h60) and 4/4 (f60). g60 (~18 bits) is NOT yet closed-form
  because Δe58 varies with W[57]; needs marginal-distribution model.

  Tools shipped:
    - prototypes/forward_table_builder.py — extends cascade chain to round 63,
      JSONL dumps. ~24k samples/sec.
    - prototypes/hard_residue_analyzer.py — per-bit bias measurement.
    - prototypes/predict_hard_bits.py — closed-form predictor for h60+f60
      (O(1) per candidate, runs over candidates.yaml in <1s).

  Pre-screen result (results/20260424_pre_screen_35.md +
  20260424_f60_h60_prediction.md): 35 candidates ranked by total predicted
  h60+f60 hard bits. Minimum: 5 bits (3 candidates tied). MSB cert
  candidate (0x17149975) has 9 — middle of the pack.

  Empirical kissat test (results/20260424_pre_screen_kissat_test.md):
  600s budget on 3 minimum-bit candidates × 2 seeds → all TIMEOUT.
  Inconclusive at this budget; standard sr=60 takes 12h. Need 4h budgets
  to settle whether dh60 hard-bit count predicts kissat solve time.

  Open work:
    - g60 marginal-distribution prediction (~1 day to derive).
    - Backward analyzer (round-63 → round-60 inversion).
    - 24-CPU-h experiment to test pre-screen predictive power.
    - GPU box: gpu_mitm_prototype.py at N=8 (still untested).

  Run history: 6 kissat runs logged via append_run.py (all TIMEOUT @ 600s).
  Audit-failure rate: 0/6.
last_heartbeat: "2026-04-24T23:50:00Z"
heartbeat_interval_days: 14

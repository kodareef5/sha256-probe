mechanism_id: true_sr61_n32
priority: 4
status: in_flight
owner: fleet
machines_assigned: []
compute_budget_cpu_hours: 10000  # HARD CAP per GPT-5.5
cpu_hours_spent: 1800            # post-audit baseline
current_progress: |
  29 TRUE sr=61 candidates available in cnfs_n32/. ~1800 CPU-h spent post-CNF-audit
  with zero SAT. NO MORE seed farming on unchanged encodings — every run must use
  a new encoding (cascade_aux) or new candidate or be in service of a structural
  predictor experiment.

  2026-04-25 structural sweep (no compute): de58 image-size predictor across all
  36 candidates; bit-19_m51ca0b34_fill55555555 is structural extreme (image=2^8,
  24-bit compression). Multi-residual analysis: at r=60 cascade boundary only
  de58 varies (others are candidate-fixed constants). Residual dimension growth
  through r=63: full 6-component residual saturates at sample size — cascade-DP
  residual is 1-D in W57 across all candidates. Theorem 4 (da_r ≡ de_r mod 2^32
  at r=61) verified at full N=32 across 1M samples, 0 violations.

  Caveat: initial cost-estimate ("brute force in 30s") was wrong — encoder
  demands FULL slot-64 collision, not just da_61=0. 1800 CPU-h baseline is real
  problem hardness, not encoding waste.

  2026-04-25 evening — predictor validation matrix (5 candidates × 2 solvers ×
  2 budgets = 20 runs + 5+5 seed=7 replicates + 3 clean re-runs = 33 total runs).
  RESULT: de58_size and hard_bit_total_lb predictors are SEARCH-IRRELEVANT.
  Spearman ρ vs kissat 10M dec/conf:
    de58_size           = +0.000
    hard_bit_total_lb   = -0.100
  Across 12 (solver, seed, budget) cells: ALL ρ ≤ 0. NEVER positively correlated.
  Wall time (clean re-run, 5 candidates uncontended at 10M): 281-315s = 12% spread.
  Spearman ρ(de58, clean_wall) = +0.000.
  CONCLUSION: distribute future compute by candidate COVERAGE (disjoint de58
  regions per the disjointness finding) rather than RANK. bit-19's structural
  extreme is real but does NOT translate to solver advantage.
  Verdict: bets/sr61_n32/results/20260425_de58_validation_VERDICT.md.

  2026-04-26 — exhaustive full 2^32 sweep at bit=31 fill=0xff: 2 eligible m0
  found, BOTH already registered (m=0xa22dc6c7 cert + m=0x17149975). Registry
  is EXHAUSTIVE for this (kernel_bit, fill) cell. Σ1/σ1 alignment hypothesis
  (registry/notes/20260425_covered_bits_pattern.md) still being tested at
  σ0-aligned bits {7, 18} and Σ0-aligned bit 22.
last_heartbeat: "2026-04-26T00:38:00Z"
heartbeat_interval_days: 3      # higher cadence — this is the most-likely-to-drift bet
audit_required: true
